// Code generated by BobGen psql v0.50.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dberrors

import (
	"errors"
	"testing"

	"github.com/jackc/pgx/v5/pgconn"
)

func TestCheckConstraintErrors(t *testing.T) {
	newCheckErr := func(code, constraintName string) error {
		return &pgconn.PgError{Code: code, ConstraintName: constraintName}
	}

	checkErr := newCheckErr("23514", "some_constraint")
	if !errors.Is(ErrCheckConstraint, checkErr) {
		t.Fatal("expected ErrCheckConstraint to match check violation")
	}
	if !ErrCheckConstraint.Is(checkErr) {
		t.Fatal("expected ErrCheckConstraint.Is to match check violation")
	}

	uniqueErr := newCheckErr("23505", "some_constraint")
	if errors.Is(ErrCheckConstraint, uniqueErr) {
		t.Fatal("expected ErrCheckConstraint not to match unique violation")
	}
	if ErrCheckConstraint.Is(uniqueErr) {
		t.Fatal("expected ErrCheckConstraint.Is not to match unique violation")
	}

	t.Run("BookingEntry_ErrCheckBookingEntriesSourceTypeCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "booking_entries_source_type_check")
		if !errors.Is(BookingEntryErrors.ErrCheckBookingEntriesSourceTypeCheck, matchingErr) {
			t.Fatalf("expected ErrCheckBookingEntriesSourceTypeCheck to match constraint %q", "booking_entries_source_type_check")
		}
		if !BookingEntryErrors.ErrCheckBookingEntriesSourceTypeCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckBookingEntriesSourceTypeCheck.Is to match constraint %q", "booking_entries_source_type_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(BookingEntryErrors.ErrCheckBookingEntriesSourceTypeCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckBookingEntriesSourceTypeCheck not to match different constraint")
		}
		if BookingEntryErrors.ErrCheckBookingEntriesSourceTypeCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckBookingEntriesSourceTypeCheck.Is not to match different constraint")
		}
	})
	t.Run("BookingEntry_ErrCheckBookingEntriesTargetTypeCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "booking_entries_target_type_check")
		if !errors.Is(BookingEntryErrors.ErrCheckBookingEntriesTargetTypeCheck, matchingErr) {
			t.Fatalf("expected ErrCheckBookingEntriesTargetTypeCheck to match constraint %q", "booking_entries_target_type_check")
		}
		if !BookingEntryErrors.ErrCheckBookingEntriesTargetTypeCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckBookingEntriesTargetTypeCheck.Is to match constraint %q", "booking_entries_target_type_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(BookingEntryErrors.ErrCheckBookingEntriesTargetTypeCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckBookingEntriesTargetTypeCheck not to match different constraint")
		}
		if BookingEntryErrors.ErrCheckBookingEntriesTargetTypeCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckBookingEntriesTargetTypeCheck.Is not to match different constraint")
		}
	})

	t.Run("Event_ErrCheckEventsProcessingStateCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "events_processing_state_check")
		if !errors.Is(EventErrors.ErrCheckEventsProcessingStateCheck, matchingErr) {
			t.Fatalf("expected ErrCheckEventsProcessingStateCheck to match constraint %q", "events_processing_state_check")
		}
		if !EventErrors.ErrCheckEventsProcessingStateCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckEventsProcessingStateCheck.Is to match constraint %q", "events_processing_state_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(EventErrors.ErrCheckEventsProcessingStateCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckEventsProcessingStateCheck not to match different constraint")
		}
		if EventErrors.ErrCheckEventsProcessingStateCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckEventsProcessingStateCheck.Is not to match different constraint")
		}
	})
	t.Run("Event_ErrCheckEventsStatusCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "events_status_check")
		if !errors.Is(EventErrors.ErrCheckEventsStatusCheck, matchingErr) {
			t.Fatalf("expected ErrCheckEventsStatusCheck to match constraint %q", "events_status_check")
		}
		if !EventErrors.ErrCheckEventsStatusCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckEventsStatusCheck.Is to match constraint %q", "events_status_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(EventErrors.ErrCheckEventsStatusCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckEventsStatusCheck not to match different constraint")
		}
		if EventErrors.ErrCheckEventsStatusCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckEventsStatusCheck.Is not to match different constraint")
		}
	})

	t.Run("ImportBatch_ErrCheckImportBatchesImportFormatCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "import_batches_import_format_check")
		if !errors.Is(ImportBatchErrors.ErrCheckImportBatchesImportFormatCheck, matchingErr) {
			t.Fatalf("expected ErrCheckImportBatchesImportFormatCheck to match constraint %q", "import_batches_import_format_check")
		}
		if !ImportBatchErrors.ErrCheckImportBatchesImportFormatCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckImportBatchesImportFormatCheck.Is to match constraint %q", "import_batches_import_format_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ImportBatchErrors.ErrCheckImportBatchesImportFormatCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckImportBatchesImportFormatCheck not to match different constraint")
		}
		if ImportBatchErrors.ErrCheckImportBatchesImportFormatCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckImportBatchesImportFormatCheck.Is not to match different constraint")
		}
	})
	t.Run("ImportBatch_ErrCheckImportBatchesProcessingStateCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "import_batches_processing_state_check")
		if !errors.Is(ImportBatchErrors.ErrCheckImportBatchesProcessingStateCheck, matchingErr) {
			t.Fatalf("expected ErrCheckImportBatchesProcessingStateCheck to match constraint %q", "import_batches_processing_state_check")
		}
		if !ImportBatchErrors.ErrCheckImportBatchesProcessingStateCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckImportBatchesProcessingStateCheck.Is to match constraint %q", "import_batches_processing_state_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ImportBatchErrors.ErrCheckImportBatchesProcessingStateCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckImportBatchesProcessingStateCheck not to match different constraint")
		}
		if ImportBatchErrors.ErrCheckImportBatchesProcessingStateCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckImportBatchesProcessingStateCheck.Is not to match different constraint")
		}
	})

	t.Run("PointSystem_ErrCheckPointSystemsRaceDistancePctCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "point_systems_race_distance_pct_check")
		if !errors.Is(PointSystemErrors.ErrCheckPointSystemsRaceDistancePctCheck, matchingErr) {
			t.Fatalf("expected ErrCheckPointSystemsRaceDistancePctCheck to match constraint %q", "point_systems_race_distance_pct_check")
		}
		if !PointSystemErrors.ErrCheckPointSystemsRaceDistancePctCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckPointSystemsRaceDistancePctCheck.Is to match constraint %q", "point_systems_race_distance_pct_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(PointSystemErrors.ErrCheckPointSystemsRaceDistancePctCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckPointSystemsRaceDistancePctCheck not to match different constraint")
		}
		if PointSystemErrors.ErrCheckPointSystemsRaceDistancePctCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckPointSystemsRaceDistancePctCheck.Is not to match different constraint")
		}
	})

	t.Run("ResultEntry_ErrCheckResultEntriesFastestLapTimeMsCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "result_entries_fastest_lap_time_ms_check")
		if !errors.Is(ResultEntryErrors.ErrCheckResultEntriesFastestLapTimeMsCheck, matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesFastestLapTimeMsCheck to match constraint %q", "result_entries_fastest_lap_time_ms_check")
		}
		if !ResultEntryErrors.ErrCheckResultEntriesFastestLapTimeMsCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesFastestLapTimeMsCheck.Is to match constraint %q", "result_entries_fastest_lap_time_ms_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ResultEntryErrors.ErrCheckResultEntriesFastestLapTimeMsCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesFastestLapTimeMsCheck not to match different constraint")
		}
		if ResultEntryErrors.ErrCheckResultEntriesFastestLapTimeMsCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesFastestLapTimeMsCheck.Is not to match different constraint")
		}
	})
	t.Run("ResultEntry_ErrCheckResultEntriesIncidentsCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "result_entries_incidents_check")
		if !errors.Is(ResultEntryErrors.ErrCheckResultEntriesIncidentsCheck, matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesIncidentsCheck to match constraint %q", "result_entries_incidents_check")
		}
		if !ResultEntryErrors.ErrCheckResultEntriesIncidentsCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesIncidentsCheck.Is to match constraint %q", "result_entries_incidents_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ResultEntryErrors.ErrCheckResultEntriesIncidentsCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesIncidentsCheck not to match different constraint")
		}
		if ResultEntryErrors.ErrCheckResultEntriesIncidentsCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesIncidentsCheck.Is not to match different constraint")
		}
	})
	t.Run("ResultEntry_ErrCheckResultEntriesLapsCompletedCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "result_entries_laps_completed_check")
		if !errors.Is(ResultEntryErrors.ErrCheckResultEntriesLapsCompletedCheck, matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesLapsCompletedCheck to match constraint %q", "result_entries_laps_completed_check")
		}
		if !ResultEntryErrors.ErrCheckResultEntriesLapsCompletedCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesLapsCompletedCheck.Is to match constraint %q", "result_entries_laps_completed_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ResultEntryErrors.ErrCheckResultEntriesLapsCompletedCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesLapsCompletedCheck not to match different constraint")
		}
		if ResultEntryErrors.ErrCheckResultEntriesLapsCompletedCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesLapsCompletedCheck.Is not to match different constraint")
		}
	})
	t.Run("ResultEntry_ErrCheckResultEntriesQualiLapTimeMsCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "result_entries_quali_lap_time_ms_check")
		if !errors.Is(ResultEntryErrors.ErrCheckResultEntriesQualiLapTimeMsCheck, matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesQualiLapTimeMsCheck to match constraint %q", "result_entries_quali_lap_time_ms_check")
		}
		if !ResultEntryErrors.ErrCheckResultEntriesQualiLapTimeMsCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesQualiLapTimeMsCheck.Is to match constraint %q", "result_entries_quali_lap_time_ms_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ResultEntryErrors.ErrCheckResultEntriesQualiLapTimeMsCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesQualiLapTimeMsCheck not to match different constraint")
		}
		if ResultEntryErrors.ErrCheckResultEntriesQualiLapTimeMsCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesQualiLapTimeMsCheck.Is not to match different constraint")
		}
	})
	t.Run("ResultEntry_ErrCheckResultEntriesStateCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "result_entries_state_check")
		if !errors.Is(ResultEntryErrors.ErrCheckResultEntriesStateCheck, matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesStateCheck to match constraint %q", "result_entries_state_check")
		}
		if !ResultEntryErrors.ErrCheckResultEntriesStateCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesStateCheck.Is to match constraint %q", "result_entries_state_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ResultEntryErrors.ErrCheckResultEntriesStateCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesStateCheck not to match different constraint")
		}
		if ResultEntryErrors.ErrCheckResultEntriesStateCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesStateCheck.Is not to match different constraint")
		}
	})
	t.Run("ResultEntry_ErrCheckResultEntriesTotalTimeMsCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "result_entries_total_time_ms_check")
		if !errors.Is(ResultEntryErrors.ErrCheckResultEntriesTotalTimeMsCheck, matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesTotalTimeMsCheck to match constraint %q", "result_entries_total_time_ms_check")
		}
		if !ResultEntryErrors.ErrCheckResultEntriesTotalTimeMsCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckResultEntriesTotalTimeMsCheck.Is to match constraint %q", "result_entries_total_time_ms_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(ResultEntryErrors.ErrCheckResultEntriesTotalTimeMsCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesTotalTimeMsCheck not to match different constraint")
		}
		if ResultEntryErrors.ErrCheckResultEntriesTotalTimeMsCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckResultEntriesTotalTimeMsCheck.Is not to match different constraint")
		}
	})

	t.Run("Season_ErrCheckSeasonsDateOrderCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "seasons_date_order_check")
		if !errors.Is(SeasonErrors.ErrCheckSeasonsDateOrderCheck, matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsDateOrderCheck to match constraint %q", "seasons_date_order_check")
		}
		if !SeasonErrors.ErrCheckSeasonsDateOrderCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsDateOrderCheck.Is to match constraint %q", "seasons_date_order_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(SeasonErrors.ErrCheckSeasonsDateOrderCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsDateOrderCheck not to match different constraint")
		}
		if SeasonErrors.ErrCheckSeasonsDateOrderCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsDateOrderCheck.Is not to match different constraint")
		}
	})
	t.Run("Season_ErrCheckSeasonsSkipEventsCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "seasons_skip_events_check")
		if !errors.Is(SeasonErrors.ErrCheckSeasonsSkipEventsCheck, matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsSkipEventsCheck to match constraint %q", "seasons_skip_events_check")
		}
		if !SeasonErrors.ErrCheckSeasonsSkipEventsCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsSkipEventsCheck.Is to match constraint %q", "seasons_skip_events_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(SeasonErrors.ErrCheckSeasonsSkipEventsCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsSkipEventsCheck not to match different constraint")
		}
		if SeasonErrors.ErrCheckSeasonsSkipEventsCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsSkipEventsCheck.Is not to match different constraint")
		}
	})
	t.Run("Season_ErrCheckSeasonsStatusCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "seasons_status_check")
		if !errors.Is(SeasonErrors.ErrCheckSeasonsStatusCheck, matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsStatusCheck to match constraint %q", "seasons_status_check")
		}
		if !SeasonErrors.ErrCheckSeasonsStatusCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsStatusCheck.Is to match constraint %q", "seasons_status_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(SeasonErrors.ErrCheckSeasonsStatusCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsStatusCheck not to match different constraint")
		}
		if SeasonErrors.ErrCheckSeasonsStatusCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsStatusCheck.Is not to match different constraint")
		}
	})
	t.Run("Season_ErrCheckSeasonsTeamPointsTopNCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "seasons_team_points_top_n_check")
		if !errors.Is(SeasonErrors.ErrCheckSeasonsTeamPointsTopNCheck, matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsTeamPointsTopNCheck to match constraint %q", "seasons_team_points_top_n_check")
		}
		if !SeasonErrors.ErrCheckSeasonsTeamPointsTopNCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckSeasonsTeamPointsTopNCheck.Is to match constraint %q", "seasons_team_points_top_n_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(SeasonErrors.ErrCheckSeasonsTeamPointsTopNCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsTeamPointsTopNCheck not to match different constraint")
		}
		if SeasonErrors.ErrCheckSeasonsTeamPointsTopNCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckSeasonsTeamPointsTopNCheck.Is not to match different constraint")
		}
	})

	t.Run("TeamDriver_ErrCheckTeamDriversLeftAtCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "team_drivers_left_at_check")
		if !errors.Is(TeamDriverErrors.ErrCheckTeamDriversLeftAtCheck, matchingErr) {
			t.Fatalf("expected ErrCheckTeamDriversLeftAtCheck to match constraint %q", "team_drivers_left_at_check")
		}
		if !TeamDriverErrors.ErrCheckTeamDriversLeftAtCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckTeamDriversLeftAtCheck.Is to match constraint %q", "team_drivers_left_at_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(TeamDriverErrors.ErrCheckTeamDriversLeftAtCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckTeamDriversLeftAtCheck not to match different constraint")
		}
		if TeamDriverErrors.ErrCheckTeamDriversLeftAtCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckTeamDriversLeftAtCheck.Is not to match different constraint")
		}
	})

	t.Run("Team_ErrCheckTeamsLeftAtCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "teams_left_at_check")
		if !errors.Is(TeamErrors.ErrCheckTeamsLeftAtCheck, matchingErr) {
			t.Fatalf("expected ErrCheckTeamsLeftAtCheck to match constraint %q", "teams_left_at_check")
		}
		if !TeamErrors.ErrCheckTeamsLeftAtCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckTeamsLeftAtCheck.Is to match constraint %q", "teams_left_at_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(TeamErrors.ErrCheckTeamsLeftAtCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckTeamsLeftAtCheck not to match different constraint")
		}
		if TeamErrors.ErrCheckTeamsLeftAtCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckTeamsLeftAtCheck.Is not to match different constraint")
		}
	})

	t.Run("TrackLayout_ErrCheckTrackLayoutsLengthMetersCheck", func(t *testing.T) {
		matchingErr := newCheckErr("23514", "track_layouts_length_meters_check")
		if !errors.Is(TrackLayoutErrors.ErrCheckTrackLayoutsLengthMetersCheck, matchingErr) {
			t.Fatalf("expected ErrCheckTrackLayoutsLengthMetersCheck to match constraint %q", "track_layouts_length_meters_check")
		}
		if !TrackLayoutErrors.ErrCheckTrackLayoutsLengthMetersCheck.Is(matchingErr) {
			t.Fatalf("expected ErrCheckTrackLayoutsLengthMetersCheck.Is to match constraint %q", "track_layouts_length_meters_check")
		}

		nonMatchingErr := newCheckErr("23514", "other_constraint")
		if errors.Is(TrackLayoutErrors.ErrCheckTrackLayoutsLengthMetersCheck, nonMatchingErr) {
			t.Fatal("expected ErrCheckTrackLayoutsLengthMetersCheck not to match different constraint")
		}
		if TrackLayoutErrors.ErrCheckTrackLayoutsLengthMetersCheck.Is(nonMatchingErr) {
			t.Fatal("expected ErrCheckTrackLayoutsLengthMetersCheck.Is not to match different constraint")
		}
	})
}
