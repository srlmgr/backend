// Code generated by BobGen psql v0.43.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"fmt"
	"io"
	"time"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/shopspring/decimal"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/dialect"
	"github.com/stephenafamo/bob/dialect/psql/dm"
	"github.com/stephenafamo/bob/dialect/psql/sm"
	"github.com/stephenafamo/bob/dialect/psql/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
	"github.com/stephenafamo/bob/types/pgtypes"
	"github.com/stephenafamo/scan"
)

// PointSystem is an object representing the database table.
type PointSystem struct {
	ID              int32            `db:"id,pk" `
	Name            string           `db:"name" `
	Description     null.Val[string] `db:"description" `
	IsActive        bool             `db:"is_active" `
	GuestPoints     bool             `db:"guest_points" `
	RaceDistancePCT decimal.Decimal  `db:"race_distance_pct" `
	CreatedAt       time.Time        `db:"created_at" `
	UpdatedAt       time.Time        `db:"updated_at" `
	CreatedBy       string           `db:"created_by" `
	UpdatedBy       string           `db:"updated_by" `

	R pointSystemR `db:"-" `

	C pointSystemC `db:"-" `
}

// PointSystemSlice is an alias for a slice of pointers to PointSystem.
// This should almost always be used instead of []*PointSystem.
type PointSystemSlice []*PointSystem

// PointSystems contains methods to work with the point_systems table
var PointSystems = psql.NewTablex[*PointSystem, PointSystemSlice, *PointSystemSetter]("", "point_systems", buildPointSystemColumns("point_systems"))

// PointSystemsQuery is a query on the point_systems table
type PointSystemsQuery = *psql.ViewQuery[*PointSystem, PointSystemSlice]

// pointSystemR is where relationships are stored.
type pointSystemR struct {
	PointRules PointRuleSlice // point_rules.point_rules_point_system_id_fk
	Seasons    SeasonSlice    // seasons.seasons_point_system_id_fk
}

func buildPointSystemColumns(tableName string) pointSystemColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "name", "description", "is_active", "guest_points", "race_distance_pct", "created_at", "updated_at", "created_by", "updated_by",
	)
	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}
	return pointSystemColumns{
		ColumnsExpr:     columnsExpr,
		tableAlias:      tableName,
		ID:              psql.Quote(tableName, "id"),
		Name:            psql.Quote(tableName, "name"),
		Description:     psql.Quote(tableName, "description"),
		IsActive:        psql.Quote(tableName, "is_active"),
		GuestPoints:     psql.Quote(tableName, "guest_points"),
		RaceDistancePCT: psql.Quote(tableName, "race_distance_pct"),
		CreatedAt:       psql.Quote(tableName, "created_at"),
		UpdatedAt:       psql.Quote(tableName, "updated_at"),
		CreatedBy:       psql.Quote(tableName, "created_by"),
		UpdatedBy:       psql.Quote(tableName, "updated_by"),
	}
}

type pointSystemColumns struct {
	expr.ColumnsExpr
	tableAlias      string
	ID              psql.Expression
	Name            psql.Expression
	Description     psql.Expression
	IsActive        psql.Expression
	GuestPoints     psql.Expression
	RaceDistancePCT psql.Expression
	CreatedAt       psql.Expression
	UpdatedAt       psql.Expression
	CreatedBy       psql.Expression
	UpdatedBy       psql.Expression
}

func (c pointSystemColumns) Alias() string {
	return c.tableAlias
}

func (pointSystemColumns) AliasedAs(tableName string) pointSystemColumns {
	return buildPointSystemColumns(tableName)
}

func (c pointSystemColumns) Unqualified() pointSystemColumns {
	return buildPointSystemColumns("")
}

// PointSystemSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type PointSystemSetter struct {
	ID              omit.Val[int32]           `db:"id,pk" `
	Name            omit.Val[string]          `db:"name" `
	Description     omitnull.Val[string]      `db:"description" `
	IsActive        omit.Val[bool]            `db:"is_active" `
	GuestPoints     omit.Val[bool]            `db:"guest_points" `
	RaceDistancePCT omit.Val[decimal.Decimal] `db:"race_distance_pct" `
	CreatedAt       omit.Val[time.Time]       `db:"created_at" `
	UpdatedAt       omit.Val[time.Time]       `db:"updated_at" `
	CreatedBy       omit.Val[string]          `db:"created_by" `
	UpdatedBy       omit.Val[string]          `db:"updated_by" `
}

func (s PointSystemSetter) SetColumns() []string {
	vals := make([]string, 0, 10)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.Name.IsValue() {
		vals = append(vals, "name")
	}
	if !s.Description.IsUnset() {
		vals = append(vals, "description")
	}
	if s.IsActive.IsValue() {
		vals = append(vals, "is_active")
	}
	if s.GuestPoints.IsValue() {
		vals = append(vals, "guest_points")
	}
	if s.RaceDistancePCT.IsValue() {
		vals = append(vals, "race_distance_pct")
	}
	if s.CreatedAt.IsValue() {
		vals = append(vals, "created_at")
	}
	if s.UpdatedAt.IsValue() {
		vals = append(vals, "updated_at")
	}
	if s.CreatedBy.IsValue() {
		vals = append(vals, "created_by")
	}
	if s.UpdatedBy.IsValue() {
		vals = append(vals, "updated_by")
	}
	return vals
}

func (s PointSystemSetter) Overwrite(t *PointSystem) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.Name.IsValue() {
		t.Name = s.Name.MustGet()
	}
	if !s.Description.IsUnset() {
		t.Description = s.Description.MustGetNull()
	}
	if s.IsActive.IsValue() {
		t.IsActive = s.IsActive.MustGet()
	}
	if s.GuestPoints.IsValue() {
		t.GuestPoints = s.GuestPoints.MustGet()
	}
	if s.RaceDistancePCT.IsValue() {
		t.RaceDistancePCT = s.RaceDistancePCT.MustGet()
	}
	if s.CreatedAt.IsValue() {
		t.CreatedAt = s.CreatedAt.MustGet()
	}
	if s.UpdatedAt.IsValue() {
		t.UpdatedAt = s.UpdatedAt.MustGet()
	}
	if s.CreatedBy.IsValue() {
		t.CreatedBy = s.CreatedBy.MustGet()
	}
	if s.UpdatedBy.IsValue() {
		t.UpdatedBy = s.UpdatedBy.MustGet()
	}
}

func (s *PointSystemSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return PointSystems.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	q.AppendValues(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		vals := make([]bob.Expression, 10)
		if s.ID.IsValue() {
			vals[0] = psql.Arg(s.ID.MustGet())
		} else {
			vals[0] = psql.Raw("DEFAULT")
		}

		if s.Name.IsValue() {
			vals[1] = psql.Arg(s.Name.MustGet())
		} else {
			vals[1] = psql.Raw("DEFAULT")
		}

		if !s.Description.IsUnset() {
			vals[2] = psql.Arg(s.Description.MustGetNull())
		} else {
			vals[2] = psql.Raw("DEFAULT")
		}

		if s.IsActive.IsValue() {
			vals[3] = psql.Arg(s.IsActive.MustGet())
		} else {
			vals[3] = psql.Raw("DEFAULT")
		}

		if s.GuestPoints.IsValue() {
			vals[4] = psql.Arg(s.GuestPoints.MustGet())
		} else {
			vals[4] = psql.Raw("DEFAULT")
		}

		if s.RaceDistancePCT.IsValue() {
			vals[5] = psql.Arg(s.RaceDistancePCT.MustGet())
		} else {
			vals[5] = psql.Raw("DEFAULT")
		}

		if s.CreatedAt.IsValue() {
			vals[6] = psql.Arg(s.CreatedAt.MustGet())
		} else {
			vals[6] = psql.Raw("DEFAULT")
		}

		if s.UpdatedAt.IsValue() {
			vals[7] = psql.Arg(s.UpdatedAt.MustGet())
		} else {
			vals[7] = psql.Raw("DEFAULT")
		}

		if s.CreatedBy.IsValue() {
			vals[8] = psql.Arg(s.CreatedBy.MustGet())
		} else {
			vals[8] = psql.Raw("DEFAULT")
		}

		if s.UpdatedBy.IsValue() {
			vals[9] = psql.Arg(s.UpdatedBy.MustGet())
		} else {
			vals[9] = psql.Raw("DEFAULT")
		}

		return bob.ExpressSlice(ctx, w, d, start, vals, "", ", ", "")
	}))
}

func (s PointSystemSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s PointSystemSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 10)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "id")...),
			psql.Arg(s.ID),
		}})
	}

	if s.Name.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "name")...),
			psql.Arg(s.Name),
		}})
	}

	if !s.Description.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "description")...),
			psql.Arg(s.Description),
		}})
	}

	if s.IsActive.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "is_active")...),
			psql.Arg(s.IsActive),
		}})
	}

	if s.GuestPoints.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "guest_points")...),
			psql.Arg(s.GuestPoints),
		}})
	}

	if s.RaceDistancePCT.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "race_distance_pct")...),
			psql.Arg(s.RaceDistancePCT),
		}})
	}

	if s.CreatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_at")...),
			psql.Arg(s.CreatedAt),
		}})
	}

	if s.UpdatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "updated_at")...),
			psql.Arg(s.UpdatedAt),
		}})
	}

	if s.CreatedBy.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_by")...),
			psql.Arg(s.CreatedBy),
		}})
	}

	if s.UpdatedBy.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "updated_by")...),
			psql.Arg(s.UpdatedBy),
		}})
	}

	return exprs
}

// FindPointSystem retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindPointSystem(ctx context.Context, exec bob.Executor, IDPK int32, cols ...string) (*PointSystem, error) {
	if len(cols) == 0 {
		return PointSystems.Query(
			sm.Where(PointSystems.Columns.ID.EQ(psql.Arg(IDPK))),
		).One(ctx, exec)
	}

	return PointSystems.Query(
		sm.Where(PointSystems.Columns.ID.EQ(psql.Arg(IDPK))),
		sm.Columns(PointSystems.Columns.Only(cols...)),
	).One(ctx, exec)
}

// PointSystemExists checks the presence of a single record by primary key
func PointSystemExists(ctx context.Context, exec bob.Executor, IDPK int32) (bool, error) {
	return PointSystems.Query(
		sm.Where(PointSystems.Columns.ID.EQ(psql.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after PointSystem is retrieved from the database
func (o *PointSystem) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = PointSystems.AfterSelectHooks.RunHooks(ctx, exec, PointSystemSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = PointSystems.AfterInsertHooks.RunHooks(ctx, exec, PointSystemSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = PointSystems.AfterUpdateHooks.RunHooks(ctx, exec, PointSystemSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = PointSystems.AfterDeleteHooks.RunHooks(ctx, exec, PointSystemSlice{o})
	case bob.QueryTypeMerge:
		ctx, err = PointSystems.AfterMergeHooks.RunHooks(ctx, exec, PointSystemSlice{o})
	}

	return err
}

// primaryKeyVals returns the primary key values of the PointSystem
func (o *PointSystem) primaryKeyVals() bob.Expression {
	return psql.Arg(o.ID)
}

func (o *PointSystem) pkEQ() dialect.Expression {
	return psql.Quote("point_systems", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the PointSystem
func (o *PointSystem) Update(ctx context.Context, exec bob.Executor, s *PointSystemSetter) error {
	v, err := PointSystems.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single PointSystem record with an executor
func (o *PointSystem) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := PointSystems.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the PointSystem using the executor
func (o *PointSystem) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := PointSystems.Query(
		sm.Where(PointSystems.Columns.ID.EQ(psql.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after PointSystemSlice is retrieved from the database
func (o PointSystemSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = PointSystems.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = PointSystems.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = PointSystems.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = PointSystems.AfterDeleteHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeMerge:
		ctx, err = PointSystems.AfterMergeHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o PointSystemSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return psql.Raw("NULL")
	}

	return psql.Quote("point_systems", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o PointSystemSlice) copyMatchingRows(from ...*PointSystem) {
	for i, old := range o {
		for _, new := range from {
			if new.ID != old.ID {
				continue
			}
			new.R = old.R
			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o PointSystemSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return PointSystems.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *PointSystem:
				o.copyMatchingRows(retrieved)
			case []*PointSystem:
				o.copyMatchingRows(retrieved...)
			case PointSystemSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a PointSystem or a slice of PointSystem
				// then run the AfterUpdateHooks on the slice
				_, err = PointSystems.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o PointSystemSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return PointSystems.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *PointSystem:
				o.copyMatchingRows(retrieved)
			case []*PointSystem:
				o.copyMatchingRows(retrieved...)
			case PointSystemSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a PointSystem or a slice of PointSystem
				// then run the AfterDeleteHooks on the slice
				_, err = PointSystems.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// MergeMod modifies a merge query to run BeforeMergeHooks and AfterMergeHooks
// and updates the slice with the returned rows.
func (o PointSystemSlice) MergeMod() bob.Mod[*dialect.MergeQuery] {
	return bob.ModFunc[*dialect.MergeQuery](func(q *dialect.MergeQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return PointSystems.BeforeMergeHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *PointSystem:
				o.copyMatchingRows(retrieved)
			case []*PointSystem:
				o.copyMatchingRows(retrieved...)
			case PointSystemSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a PointSystem or a slice of PointSystem
				// then run the AfterMergeHooks on the slice
				_, err = PointSystems.AfterMergeHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))
	})
}

func (o PointSystemSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals PointSystemSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := PointSystems.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o PointSystemSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := PointSystems.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o PointSystemSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := PointSystems.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// PointRules starts a query for related objects on point_rules
func (o *PointSystem) PointRules(mods ...bob.Mod[*dialect.SelectQuery]) PointRulesQuery {
	return PointRules.Query(append(mods,
		sm.Where(PointRules.Columns.PointSystemID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os PointSystemSlice) PointRules(mods ...bob.Mod[*dialect.SelectQuery]) PointRulesQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	return PointRules.Query(append(mods,
		sm.Where(psql.Group(PointRules.Columns.PointSystemID).OP("IN", PKArgExpr)),
	)...)
}

// Seasons starts a query for related objects on seasons
func (o *PointSystem) Seasons(mods ...bob.Mod[*dialect.SelectQuery]) SeasonsQuery {
	return Seasons.Query(append(mods,
		sm.Where(Seasons.Columns.PointSystemID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os PointSystemSlice) Seasons(mods ...bob.Mod[*dialect.SelectQuery]) SeasonsQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	return Seasons.Query(append(mods,
		sm.Where(psql.Group(Seasons.Columns.PointSystemID).OP("IN", PKArgExpr)),
	)...)
}

func insertPointSystemPointRules0(ctx context.Context, exec bob.Executor, pointRules1 []*PointRuleSetter, pointSystem0 *PointSystem) (PointRuleSlice, error) {
	for i := range pointRules1 {
		pointRules1[i].PointSystemID = omit.From(pointSystem0.ID)
	}

	ret, err := PointRules.Insert(bob.ToMods(pointRules1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertPointSystemPointRules0: %w", err)
	}

	return ret, nil
}

func attachPointSystemPointRules0(ctx context.Context, exec bob.Executor, count int, pointRules1 PointRuleSlice, pointSystem0 *PointSystem) (PointRuleSlice, error) {
	setter := &PointRuleSetter{
		PointSystemID: omit.From(pointSystem0.ID),
	}

	err := pointRules1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachPointSystemPointRules0: %w", err)
	}

	return pointRules1, nil
}

func (pointSystem0 *PointSystem) InsertPointRules(ctx context.Context, exec bob.Executor, related ...*PointRuleSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	pointRules1, err := insertPointSystemPointRules0(ctx, exec, related, pointSystem0)
	if err != nil {
		return err
	}

	pointSystem0.R.PointRules = append(pointSystem0.R.PointRules, pointRules1...)

	for _, rel := range pointRules1 {
		rel.R.PointSystem = pointSystem0
	}
	return nil
}

func (pointSystem0 *PointSystem) AttachPointRules(ctx context.Context, exec bob.Executor, related ...*PointRule) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	pointRules1 := PointRuleSlice(related)

	_, err = attachPointSystemPointRules0(ctx, exec, len(related), pointRules1, pointSystem0)
	if err != nil {
		return err
	}

	pointSystem0.R.PointRules = append(pointSystem0.R.PointRules, pointRules1...)

	for _, rel := range related {
		rel.R.PointSystem = pointSystem0
	}

	return nil
}

func insertPointSystemSeasons0(ctx context.Context, exec bob.Executor, seasons1 []*SeasonSetter, pointSystem0 *PointSystem) (SeasonSlice, error) {
	for i := range seasons1 {
		seasons1[i].PointSystemID = omit.From(pointSystem0.ID)
	}

	ret, err := Seasons.Insert(bob.ToMods(seasons1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertPointSystemSeasons0: %w", err)
	}

	return ret, nil
}

func attachPointSystemSeasons0(ctx context.Context, exec bob.Executor, count int, seasons1 SeasonSlice, pointSystem0 *PointSystem) (SeasonSlice, error) {
	setter := &SeasonSetter{
		PointSystemID: omit.From(pointSystem0.ID),
	}

	err := seasons1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachPointSystemSeasons0: %w", err)
	}

	return seasons1, nil
}

func (pointSystem0 *PointSystem) InsertSeasons(ctx context.Context, exec bob.Executor, related ...*SeasonSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	seasons1, err := insertPointSystemSeasons0(ctx, exec, related, pointSystem0)
	if err != nil {
		return err
	}

	pointSystem0.R.Seasons = append(pointSystem0.R.Seasons, seasons1...)

	for _, rel := range seasons1 {
		rel.R.PointSystem = pointSystem0
	}
	return nil
}

func (pointSystem0 *PointSystem) AttachSeasons(ctx context.Context, exec bob.Executor, related ...*Season) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	seasons1 := SeasonSlice(related)

	_, err = attachPointSystemSeasons0(ctx, exec, len(related), seasons1, pointSystem0)
	if err != nil {
		return err
	}

	pointSystem0.R.Seasons = append(pointSystem0.R.Seasons, seasons1...)

	for _, rel := range related {
		rel.R.PointSystem = pointSystem0
	}

	return nil
}

type pointSystemWhere[Q psql.Filterable] struct {
	ID              psql.WhereMod[Q, int32]
	Name            psql.WhereMod[Q, string]
	Description     psql.WhereNullMod[Q, string]
	IsActive        psql.WhereMod[Q, bool]
	GuestPoints     psql.WhereMod[Q, bool]
	RaceDistancePCT psql.WhereMod[Q, decimal.Decimal]
	CreatedAt       psql.WhereMod[Q, time.Time]
	UpdatedAt       psql.WhereMod[Q, time.Time]
	CreatedBy       psql.WhereMod[Q, string]
	UpdatedBy       psql.WhereMod[Q, string]
}

func (pointSystemWhere[Q]) AliasedAs(alias string) pointSystemWhere[Q] {
	return buildPointSystemWhere[Q](buildPointSystemColumns(alias))
}

func buildPointSystemWhere[Q psql.Filterable](cols pointSystemColumns) pointSystemWhere[Q] {
	return pointSystemWhere[Q]{
		ID:              psql.Where[Q, int32](cols.ID),
		Name:            psql.Where[Q, string](cols.Name),
		Description:     psql.WhereNull[Q, string](cols.Description),
		IsActive:        psql.Where[Q, bool](cols.IsActive),
		GuestPoints:     psql.Where[Q, bool](cols.GuestPoints),
		RaceDistancePCT: psql.Where[Q, decimal.Decimal](cols.RaceDistancePCT),
		CreatedAt:       psql.Where[Q, time.Time](cols.CreatedAt),
		UpdatedAt:       psql.Where[Q, time.Time](cols.UpdatedAt),
		CreatedBy:       psql.Where[Q, string](cols.CreatedBy),
		UpdatedBy:       psql.Where[Q, string](cols.UpdatedBy),
	}
}

func (o *PointSystem) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "PointRules":
		rels, ok := retrieved.(PointRuleSlice)
		if !ok {
			return fmt.Errorf("pointSystem cannot load %T as %q", retrieved, name)
		}

		o.R.PointRules = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.PointSystem = o
			}
		}
		return nil
	case "Seasons":
		rels, ok := retrieved.(SeasonSlice)
		if !ok {
			return fmt.Errorf("pointSystem cannot load %T as %q", retrieved, name)
		}

		o.R.Seasons = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.PointSystem = o
			}
		}
		return nil
	default:
		return fmt.Errorf("pointSystem has no relationship %q", name)
	}
}

type pointSystemPreloader struct{}

func buildPointSystemPreloader() pointSystemPreloader {
	return pointSystemPreloader{}
}

type pointSystemThenLoader[Q orm.Loadable] struct {
	PointRules func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Seasons    func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildPointSystemThenLoader[Q orm.Loadable]() pointSystemThenLoader[Q] {
	type PointRulesLoadInterface interface {
		LoadPointRules(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SeasonsLoadInterface interface {
		LoadSeasons(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return pointSystemThenLoader[Q]{
		PointRules: thenLoadBuilder[Q](
			"PointRules",
			func(ctx context.Context, exec bob.Executor, retrieved PointRulesLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadPointRules(ctx, exec, mods...)
			},
		),
		Seasons: thenLoadBuilder[Q](
			"Seasons",
			func(ctx context.Context, exec bob.Executor, retrieved SeasonsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadSeasons(ctx, exec, mods...)
			},
		),
	}
}

// LoadPointRules loads the pointSystem's PointRules into the .R struct
func (o *PointSystem) LoadPointRules(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.PointRules = nil

	related, err := o.PointRules(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.PointSystem = o
	}

	o.R.PointRules = related
	return nil
}

// LoadPointRules loads the pointSystem's PointRules into the .R struct
func (os PointSystemSlice) LoadPointRules(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	pointRules, err := os.PointRules(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.PointRules = nil
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range pointRules {

			if !(o.ID == rel.PointSystemID) {
				continue
			}

			rel.R.PointSystem = o

			o.R.PointRules = append(o.R.PointRules, rel)
		}
	}

	return nil
}

// LoadSeasons loads the pointSystem's Seasons into the .R struct
func (o *PointSystem) LoadSeasons(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Seasons = nil

	related, err := o.Seasons(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.PointSystem = o
	}

	o.R.Seasons = related
	return nil
}

// LoadSeasons loads the pointSystem's Seasons into the .R struct
func (os PointSystemSlice) LoadSeasons(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	seasons, err := os.Seasons(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.Seasons = nil
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range seasons {

			if !(o.ID == rel.PointSystemID) {
				continue
			}

			rel.R.PointSystem = o

			o.R.Seasons = append(o.R.Seasons, rel)
		}
	}

	return nil
}

// pointSystemC is where relationship counts are stored.
type pointSystemC struct {
	PointRules *int64
	Seasons    *int64
}

// PreloadCount sets a count in the C struct by name
func (o *PointSystem) PreloadCount(name string, count int64) error {
	if o == nil {
		return nil
	}

	switch name {
	case "PointRules":
		o.C.PointRules = &count
	case "Seasons":
		o.C.Seasons = &count
	}
	return nil
}

type pointSystemCountPreloader struct {
	PointRules func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	Seasons    func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
}

func buildPointSystemCountPreloader() pointSystemCountPreloader {
	return pointSystemCountPreloader{
		PointRules: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*PointSystem]("PointRules", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = PointSystems.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(PointRules.Name()),
					sm.Where(psql.Quote(PointRules.Alias(), "point_system_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		Seasons: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*PointSystem]("Seasons", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = PointSystems.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(Seasons.Name()),
					sm.Where(psql.Quote(Seasons.Alias(), "point_system_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
	}
}

type pointSystemCountThenLoader[Q orm.Loadable] struct {
	PointRules func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Seasons    func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildPointSystemCountThenLoader[Q orm.Loadable]() pointSystemCountThenLoader[Q] {
	type PointRulesCountInterface interface {
		LoadCountPointRules(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SeasonsCountInterface interface {
		LoadCountSeasons(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return pointSystemCountThenLoader[Q]{
		PointRules: countThenLoadBuilder[Q](
			"PointRules",
			func(ctx context.Context, exec bob.Executor, retrieved PointRulesCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountPointRules(ctx, exec, mods...)
			},
		),
		Seasons: countThenLoadBuilder[Q](
			"Seasons",
			func(ctx context.Context, exec bob.Executor, retrieved SeasonsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountSeasons(ctx, exec, mods...)
			},
		),
	}
}

// LoadCountPointRules loads the count of PointRules into the C struct
func (o *PointSystem) LoadCountPointRules(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.PointRules(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.PointRules = &count
	return nil
}

// LoadCountPointRules loads the count of PointRules for a slice in a single batch query
func (os PointSystemSlice) LoadCountPointRules(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			PointRules.Columns.PointSystemID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(PointRules.NameAs()),

		// WHERE fk IN (parent PKs)
		sm.Where(PointRules.Columns.PointSystemID.OP("IN", PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(PointRules.Columns.PointSystemID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.PointRules = &count
	}

	return nil
}

// LoadCountSeasons loads the count of Seasons into the C struct
func (o *PointSystem) LoadCountSeasons(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.Seasons(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.Seasons = &count
	return nil
}

// LoadCountSeasons loads the count of Seasons for a slice in a single batch query
func (os PointSystemSlice) LoadCountSeasons(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			Seasons.Columns.PointSystemID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(Seasons.NameAs()),

		// WHERE fk IN (parent PKs)
		sm.Where(Seasons.Columns.PointSystemID.OP("IN", PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(Seasons.Columns.PointSystemID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.Seasons = &count
	}

	return nil
}

type pointSystemJoins[Q dialect.Joinable] struct {
	typ        string
	PointRules modAs[Q, pointRuleColumns]
	Seasons    modAs[Q, seasonColumns]
}

func (j pointSystemJoins[Q]) aliasedAs(alias string) pointSystemJoins[Q] {
	return buildPointSystemJoins[Q](buildPointSystemColumns(alias), j.typ)
}

func buildPointSystemJoins[Q dialect.Joinable](cols pointSystemColumns, typ string) pointSystemJoins[Q] {
	return pointSystemJoins[Q]{
		typ: typ,
		PointRules: modAs[Q, pointRuleColumns]{
			c: PointRules.Columns,
			f: func(to pointRuleColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, PointRules.Name().As(to.Alias())).On(
						to.PointSystemID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		Seasons: modAs[Q, seasonColumns]{
			c: Seasons.Columns,
			f: func(to seasonColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Seasons.Name().As(to.Alias())).On(
						to.PointSystemID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
	}
}
