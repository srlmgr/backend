// Code generated by BobGen psql v0.49.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"strings"
	"testing"

	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/sm"
)

// TestBookingEntryHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestBookingEntryHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarClass", func(t *testing.T) {
		q := psql.Select(
			sm.From(BookingEntries.NameExpr()),
			SelectWhere.BookingEntries.R.HasCarClass(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarClass: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarClass: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(BookingEntries.NameExpr()),
			SelectWhere.BookingEntries.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Event", func(t *testing.T) {
		q := psql.Select(
			sm.From(BookingEntries.NameExpr()),
			SelectWhere.BookingEntries.R.HasEvent(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvent: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvent: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("RaceGrid", func(t *testing.T) {
		q := psql.Select(
			sm.From(BookingEntries.NameExpr()),
			SelectWhere.BookingEntries.R.HasRaceGrid(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRaceGrid: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRaceGrid: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Race", func(t *testing.T) {
		q := psql.Select(
			sm.From(BookingEntries.NameExpr()),
			SelectWhere.BookingEntries.R.HasRace(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRace: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRace: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Team", func(t *testing.T) {
		q := psql.Select(
			sm.From(BookingEntries.NameExpr()),
			SelectWhere.BookingEntries.R.HasTeam(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeam: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeam: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestCarClassHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestCarClassHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("BookingEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarClasses.NameExpr()),
			SelectWhere.CarClasses.R.HasBookingEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasBookingEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasBookingEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CarClassesToCarModels", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarClasses.NameExpr()),
			SelectWhere.CarClasses.R.HasCarClassesToCarModels(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarClassesToCarModels: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarClassesToCarModels: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ResultEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarClasses.NameExpr()),
			SelectWhere.CarClasses.R.HasResultEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasResultEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasResultEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonCarClasses", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarClasses.NameExpr()),
			SelectWhere.CarClasses.R.HasSeasonCarClasses(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonCarClasses: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonCarClasses: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestCarClassesToCarModelHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestCarClassesToCarModelHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarClass", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarClassesToCarModels.NameExpr()),
			SelectWhere.CarClassesToCarModels.R.HasCarClass(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarClass: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarClass: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CarModelVariant", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarClassesToCarModels.NameExpr()),
			SelectWhere.CarClassesToCarModels.R.HasCarModelVariant(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariant: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariant: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestCarManufacturerHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestCarManufacturerHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("ManufacturerCarModels", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarManufacturers.NameExpr()),
			SelectWhere.CarManufacturers.R.HasManufacturerCarModels(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasManufacturerCarModels: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasManufacturerCarModels: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestCarModelVariantHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestCarModelVariantHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarClassesToCarModels", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasCarClassesToCarModels(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarClassesToCarModels: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarClassesToCarModels: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CarModel", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasCarModel(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModel: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModel: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ResultEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasResultEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasResultEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasResultEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonCarModelVariants", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasSeasonCarModelVariants(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonCarModelVariants: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonCarModelVariants: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonDrivers", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasSeasonDrivers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonDrivers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonDrivers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationCarAliases", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasSimulationCarAliases(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationCarAliases: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationCarAliases: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Teams", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModelVariants.NameExpr()),
			SelectWhere.CarModelVariants.R.HasTeams(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeams: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeams: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestCarModelHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestCarModelHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarModelVariants", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModels.NameExpr()),
			SelectWhere.CarModels.R.HasCarModelVariants(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariants: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariants: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ManufacturerCarManufacturer", func(t *testing.T) {
		q := psql.Select(
			sm.From(CarModels.NameExpr()),
			SelectWhere.CarModels.R.HasManufacturerCarManufacturer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasManufacturerCarManufacturer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasManufacturerCarManufacturer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestDriverHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestDriverHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("BookingEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasBookingEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasBookingEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasBookingEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("EventDriverStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasEventDriverStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventDriverStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventDriverStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ResultEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasResultEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasResultEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasResultEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonDriverStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasSeasonDriverStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonDriverStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonDriverStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonDrivers", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasSeasonDrivers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonDrivers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonDrivers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationDriverAliases", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasSimulationDriverAliases(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationDriverAliases: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationDriverAliases: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("TeamDrivers", func(t *testing.T) {
		q := psql.Select(
			sm.From(Drivers.NameExpr()),
			SelectWhere.Drivers.R.HasTeamDrivers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeamDrivers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeamDrivers: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestEventDriverStandingHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestEventDriverStandingHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventDriverStandings.NameExpr()),
			SelectWhere.EventDriverStandings.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Event", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventDriverStandings.NameExpr()),
			SelectWhere.EventDriverStandings.R.HasEvent(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvent: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvent: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventDriverStandings.NameExpr()),
			SelectWhere.EventDriverStandings.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestEventProcessingAuditHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestEventProcessingAuditHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Event", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventProcessingAudits.NameExpr()),
			SelectWhere.EventProcessingAudits.R.HasEvent(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvent: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvent: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ImportBatch", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventProcessingAudits.NameExpr()),
			SelectWhere.EventProcessingAudits.R.HasImportBatch(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasImportBatch: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasImportBatch: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestEventTeamStandingHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestEventTeamStandingHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Event", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventTeamStandings.NameExpr()),
			SelectWhere.EventTeamStandings.R.HasEvent(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvent: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvent: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventTeamStandings.NameExpr()),
			SelectWhere.EventTeamStandings.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Team", func(t *testing.T) {
		q := psql.Select(
			sm.From(EventTeamStandings.NameExpr()),
			SelectWhere.EventTeamStandings.R.HasTeam(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeam: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeam: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestEventHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestEventHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("BookingEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasBookingEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasBookingEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasBookingEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("EventDriverStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasEventDriverStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventDriverStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventDriverStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("EventProcessingAudits", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasEventProcessingAudits(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventProcessingAudits: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventProcessingAudits: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("EventTeamStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasEventTeamStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventTeamStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventTeamStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("TrackLayout", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasTrackLayout(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTrackLayout: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTrackLayout: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Races", func(t *testing.T) {
		q := psql.Select(
			sm.From(Events.NameExpr()),
			SelectWhere.Events.R.HasRaces(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRaces: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRaces: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestImportBatchHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestImportBatchHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("EventProcessingAudits", func(t *testing.T) {
		q := psql.Select(
			sm.From(ImportBatches.NameExpr()),
			SelectWhere.ImportBatches.R.HasEventProcessingAudits(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventProcessingAudits: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventProcessingAudits: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("RaceGrid", func(t *testing.T) {
		q := psql.Select(
			sm.From(ImportBatches.NameExpr()),
			SelectWhere.ImportBatches.R.HasRaceGrid(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRaceGrid: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRaceGrid: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestPointRuleHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestPointRuleHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("PointSystem", func(t *testing.T) {
		q := psql.Select(
			sm.From(PointRules.NameExpr()),
			SelectWhere.PointRules.R.HasPointSystem(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPointSystem: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPointSystem: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestPointSystemHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestPointSystemHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("PointRules", func(t *testing.T) {
		q := psql.Select(
			sm.From(PointSystems.NameExpr()),
			SelectWhere.PointSystems.R.HasPointRules(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPointRules: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPointRules: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Seasons", func(t *testing.T) {
		q := psql.Select(
			sm.From(PointSystems.NameExpr()),
			SelectWhere.PointSystems.R.HasSeasons(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasons: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasons: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRaceGridHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRaceGridHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("BookingEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(RaceGrids.NameExpr()),
			SelectWhere.RaceGrids.R.HasBookingEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasBookingEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasBookingEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ImportBatches", func(t *testing.T) {
		q := psql.Select(
			sm.From(RaceGrids.NameExpr()),
			SelectWhere.RaceGrids.R.HasImportBatches(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasImportBatches: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasImportBatches: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Race", func(t *testing.T) {
		q := psql.Select(
			sm.From(RaceGrids.NameExpr()),
			SelectWhere.RaceGrids.R.HasRace(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRace: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRace: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ResultEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(RaceGrids.NameExpr()),
			SelectWhere.RaceGrids.R.HasResultEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasResultEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasResultEntries: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRaceHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRaceHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("BookingEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(Races.NameExpr()),
			SelectWhere.Races.R.HasBookingEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasBookingEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasBookingEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("RaceGrids", func(t *testing.T) {
		q := psql.Select(
			sm.From(Races.NameExpr()),
			SelectWhere.Races.R.HasRaceGrids(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRaceGrids: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRaceGrids: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Event", func(t *testing.T) {
		q := psql.Select(
			sm.From(Races.NameExpr()),
			SelectWhere.Races.R.HasEvent(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvent: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvent: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRacingSimHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRacingSimHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("SimulationSeries", func(t *testing.T) {
		q := psql.Select(
			sm.From(RacingSims.NameExpr()),
			SelectWhere.RacingSims.R.HasSimulationSeries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationSeries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationSeries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationSimulationCarAliases", func(t *testing.T) {
		q := psql.Select(
			sm.From(RacingSims.NameExpr()),
			SelectWhere.RacingSims.R.HasSimulationSimulationCarAliases(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationSimulationCarAliases: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationSimulationCarAliases: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationSimulationDriverAliases", func(t *testing.T) {
		q := psql.Select(
			sm.From(RacingSims.NameExpr()),
			SelectWhere.RacingSims.R.HasSimulationSimulationDriverAliases(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationSimulationDriverAliases: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationSimulationDriverAliases: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationSimulationTrackLayoutAliases", func(t *testing.T) {
		q := psql.Select(
			sm.From(RacingSims.NameExpr()),
			SelectWhere.RacingSims.R.HasSimulationSimulationTrackLayoutAliases(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationSimulationTrackLayoutAliases: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationSimulationTrackLayoutAliases: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestResultEntryHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestResultEntryHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarClass", func(t *testing.T) {
		q := psql.Select(
			sm.From(ResultEntries.NameExpr()),
			SelectWhere.ResultEntries.R.HasCarClass(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarClass: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarClass: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CarModelVariant", func(t *testing.T) {
		q := psql.Select(
			sm.From(ResultEntries.NameExpr()),
			SelectWhere.ResultEntries.R.HasCarModelVariant(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariant: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariant: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(ResultEntries.NameExpr()),
			SelectWhere.ResultEntries.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("RaceGrid", func(t *testing.T) {
		q := psql.Select(
			sm.From(ResultEntries.NameExpr()),
			SelectWhere.ResultEntries.R.HasRaceGrid(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRaceGrid: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRaceGrid: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Team", func(t *testing.T) {
		q := psql.Select(
			sm.From(ResultEntries.NameExpr()),
			SelectWhere.ResultEntries.R.HasTeam(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeam: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeam: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeasonCarClassHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeasonCarClassHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarClass", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonCarClasses.NameExpr()),
			SelectWhere.SeasonCarClasses.R.HasCarClass(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarClass: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarClass: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonCarClasses.NameExpr()),
			SelectWhere.SeasonCarClasses.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeasonCarModelVariantHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeasonCarModelVariantHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarModelVariant", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonCarModelVariants.NameExpr()),
			SelectWhere.SeasonCarModelVariants.R.HasCarModelVariant(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariant: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariant: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonCarModelVariants.NameExpr()),
			SelectWhere.SeasonCarModelVariants.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeasonDriverStandingHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeasonDriverStandingHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonDriverStandings.NameExpr()),
			SelectWhere.SeasonDriverStandings.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonDriverStandings.NameExpr()),
			SelectWhere.SeasonDriverStandings.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeasonDriverHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeasonDriverHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarModelVariant", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonDrivers.NameExpr()),
			SelectWhere.SeasonDrivers.R.HasCarModelVariant(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariant: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariant: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonDrivers.NameExpr()),
			SelectWhere.SeasonDrivers.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonDrivers.NameExpr()),
			SelectWhere.SeasonDrivers.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeasonTeamStandingHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeasonTeamStandingHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonTeamStandings.NameExpr()),
			SelectWhere.SeasonTeamStandings.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Team", func(t *testing.T) {
		q := psql.Select(
			sm.From(SeasonTeamStandings.NameExpr()),
			SelectWhere.SeasonTeamStandings.R.HasTeam(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeam: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeam: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeasonHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeasonHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("EventDriverStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasEventDriverStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventDriverStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventDriverStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("EventTeamStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasEventTeamStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventTeamStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventTeamStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Events", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasEvents(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvents: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvents: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonCarClasses", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasSeasonCarClasses(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonCarClasses: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonCarClasses: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonCarModelVariants", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasSeasonCarModelVariants(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonCarModelVariants: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonCarModelVariants: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonDriverStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasSeasonDriverStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonDriverStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonDriverStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonDrivers", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasSeasonDrivers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonDrivers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonDrivers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonTeamStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasSeasonTeamStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonTeamStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonTeamStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("PointSystem", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasPointSystem(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPointSystem: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPointSystem: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Series", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasSeries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Teams", func(t *testing.T) {
		q := psql.Select(
			sm.From(Seasons.NameExpr()),
			SelectWhere.Seasons.R.HasTeams(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeams: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeams: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSeriesHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSeriesHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Seasons", func(t *testing.T) {
		q := psql.Select(
			sm.From(Serieses.NameExpr()),
			SelectWhere.Serieses.R.HasSeasons(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasons: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasons: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationRacingSim", func(t *testing.T) {
		q := psql.Select(
			sm.From(Serieses.NameExpr()),
			SelectWhere.Serieses.R.HasSimulationRacingSim(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationRacingSim: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationRacingSim: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSimulationCarAliasHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSimulationCarAliasHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CarModelVariant", func(t *testing.T) {
		q := psql.Select(
			sm.From(SimulationCarAliases.NameExpr()),
			SelectWhere.SimulationCarAliases.R.HasCarModelVariant(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariant: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariant: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationRacingSim", func(t *testing.T) {
		q := psql.Select(
			sm.From(SimulationCarAliases.NameExpr()),
			SelectWhere.SimulationCarAliases.R.HasSimulationRacingSim(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationRacingSim: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationRacingSim: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSimulationDriverAliasHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSimulationDriverAliasHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(SimulationDriverAliases.NameExpr()),
			SelectWhere.SimulationDriverAliases.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationRacingSim", func(t *testing.T) {
		q := psql.Select(
			sm.From(SimulationDriverAliases.NameExpr()),
			SelectWhere.SimulationDriverAliases.R.HasSimulationRacingSim(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationRacingSim: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationRacingSim: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSimulationTrackLayoutAliasHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSimulationTrackLayoutAliasHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("SimulationRacingSim", func(t *testing.T) {
		q := psql.Select(
			sm.From(SimulationTrackLayoutAliases.NameExpr()),
			SelectWhere.SimulationTrackLayoutAliases.R.HasSimulationRacingSim(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationRacingSim: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationRacingSim: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("TrackLayout", func(t *testing.T) {
		q := psql.Select(
			sm.From(SimulationTrackLayoutAliases.NameExpr()),
			SelectWhere.SimulationTrackLayoutAliases.R.HasTrackLayout(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTrackLayout: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTrackLayout: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestTeamDriverHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestTeamDriverHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Driver", func(t *testing.T) {
		q := psql.Select(
			sm.From(TeamDrivers.NameExpr()),
			SelectWhere.TeamDrivers.R.HasDriver(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDriver: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDriver: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Team", func(t *testing.T) {
		q := psql.Select(
			sm.From(TeamDrivers.NameExpr()),
			SelectWhere.TeamDrivers.R.HasTeam(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeam: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeam: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestTeamHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestTeamHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("BookingEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasBookingEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasBookingEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasBookingEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("EventTeamStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasEventTeamStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEventTeamStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEventTeamStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ResultEntries", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasResultEntries(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasResultEntries: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasResultEntries: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SeasonTeamStandings", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasSeasonTeamStandings(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeasonTeamStandings: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeasonTeamStandings: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("TeamDrivers", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasTeamDrivers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTeamDrivers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTeamDrivers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CarModelVariant", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasCarModelVariant(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCarModelVariant: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCarModelVariant: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Season", func(t *testing.T) {
		q := psql.Select(
			sm.From(Teams.NameExpr()),
			SelectWhere.Teams.R.HasSeason(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSeason: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSeason: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestTrackLayoutHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestTrackLayoutHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Events", func(t *testing.T) {
		q := psql.Select(
			sm.From(TrackLayouts.NameExpr()),
			SelectWhere.TrackLayouts.R.HasEvents(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasEvents: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasEvents: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SimulationTrackLayoutAliases", func(t *testing.T) {
		q := psql.Select(
			sm.From(TrackLayouts.NameExpr()),
			SelectWhere.TrackLayouts.R.HasSimulationTrackLayoutAliases(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSimulationTrackLayoutAliases: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSimulationTrackLayoutAliases: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Track", func(t *testing.T) {
		q := psql.Select(
			sm.From(TrackLayouts.NameExpr()),
			SelectWhere.TrackLayouts.R.HasTrack(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTrack: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTrack: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestTrackHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestTrackHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("TrackLayouts", func(t *testing.T) {
		q := psql.Select(
			sm.From(Tracks.NameExpr()),
			SelectWhere.Tracks.R.HasTrackLayouts(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasTrackLayouts: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasTrackLayouts: expected EXISTS in query, got: %s", sql)
		}
	})
}
