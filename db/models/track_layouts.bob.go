// Code generated by BobGen psql v0.43.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"fmt"
	"io"
	"time"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/dialect"
	"github.com/stephenafamo/bob/dialect/psql/dm"
	"github.com/stephenafamo/bob/dialect/psql/sm"
	"github.com/stephenafamo/bob/dialect/psql/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
	"github.com/stephenafamo/bob/types/pgtypes"
	"github.com/stephenafamo/scan"
)

// TrackLayout is an object representing the database table.
type TrackLayout struct {
	ID             int32            `db:"id,pk" `
	TrackID        int32            `db:"track_id" `
	Name           string           `db:"name" `
	LengthMeters   null.Val[int32]  `db:"length_meters" `
	LayoutImageURL null.Val[string] `db:"layout_image_url" `
	IsActive       bool             `db:"is_active" `
	CreatedAt      time.Time        `db:"created_at" `
	UpdatedAt      time.Time        `db:"updated_at" `
	CreatedBy      string           `db:"created_by" `
	UpdatedBy      string           `db:"updated_by" `

	R trackLayoutR `db:"-" `

	C trackLayoutC `db:"-" `
}

// TrackLayoutSlice is an alias for a slice of pointers to TrackLayout.
// This should almost always be used instead of []*TrackLayout.
type TrackLayoutSlice []*TrackLayout

// TrackLayouts contains methods to work with the track_layouts table
var TrackLayouts = psql.NewTablex[*TrackLayout, TrackLayoutSlice, *TrackLayoutSetter]("", "track_layouts", buildTrackLayoutColumns("track_layouts"))

// TrackLayoutsQuery is a query on the track_layouts table
type TrackLayoutsQuery = *psql.ViewQuery[*TrackLayout, TrackLayoutSlice]

// trackLayoutR is where relationships are stored.
type trackLayoutR struct {
	Events                       EventSlice                      // events.events_track_layout_id_fk
	SimulationTrackLayoutAliases SimulationTrackLayoutAliasSlice // simulation_track_layout_aliases.simulation_track_layout_aliases_track_layout_id_fk
	Track                        *Track                          // track_layouts.track_layouts_track_id_fk
}

func buildTrackLayoutColumns(tableName string) trackLayoutColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "track_id", "name", "length_meters", "layout_image_url", "is_active", "created_at", "updated_at", "created_by", "updated_by",
	)
	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}
	return trackLayoutColumns{
		ColumnsExpr:    columnsExpr,
		tableAlias:     tableName,
		ID:             psql.Quote(tableName, "id"),
		TrackID:        psql.Quote(tableName, "track_id"),
		Name:           psql.Quote(tableName, "name"),
		LengthMeters:   psql.Quote(tableName, "length_meters"),
		LayoutImageURL: psql.Quote(tableName, "layout_image_url"),
		IsActive:       psql.Quote(tableName, "is_active"),
		CreatedAt:      psql.Quote(tableName, "created_at"),
		UpdatedAt:      psql.Quote(tableName, "updated_at"),
		CreatedBy:      psql.Quote(tableName, "created_by"),
		UpdatedBy:      psql.Quote(tableName, "updated_by"),
	}
}

type trackLayoutColumns struct {
	expr.ColumnsExpr
	tableAlias     string
	ID             psql.Expression
	TrackID        psql.Expression
	Name           psql.Expression
	LengthMeters   psql.Expression
	LayoutImageURL psql.Expression
	IsActive       psql.Expression
	CreatedAt      psql.Expression
	UpdatedAt      psql.Expression
	CreatedBy      psql.Expression
	UpdatedBy      psql.Expression
}

func (c trackLayoutColumns) Alias() string {
	return c.tableAlias
}

func (trackLayoutColumns) AliasedAs(tableName string) trackLayoutColumns {
	return buildTrackLayoutColumns(tableName)
}

func (c trackLayoutColumns) Unqualified() trackLayoutColumns {
	return buildTrackLayoutColumns("")
}

// TrackLayoutSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type TrackLayoutSetter struct {
	ID             omit.Val[int32]      `db:"id,pk" `
	TrackID        omit.Val[int32]      `db:"track_id" `
	Name           omit.Val[string]     `db:"name" `
	LengthMeters   omitnull.Val[int32]  `db:"length_meters" `
	LayoutImageURL omitnull.Val[string] `db:"layout_image_url" `
	IsActive       omit.Val[bool]       `db:"is_active" `
	CreatedAt      omit.Val[time.Time]  `db:"created_at" `
	UpdatedAt      omit.Val[time.Time]  `db:"updated_at" `
	CreatedBy      omit.Val[string]     `db:"created_by" `
	UpdatedBy      omit.Val[string]     `db:"updated_by" `
}

func (s TrackLayoutSetter) SetColumns() []string {
	vals := make([]string, 0, 10)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.TrackID.IsValue() {
		vals = append(vals, "track_id")
	}
	if s.Name.IsValue() {
		vals = append(vals, "name")
	}
	if !s.LengthMeters.IsUnset() {
		vals = append(vals, "length_meters")
	}
	if !s.LayoutImageURL.IsUnset() {
		vals = append(vals, "layout_image_url")
	}
	if s.IsActive.IsValue() {
		vals = append(vals, "is_active")
	}
	if s.CreatedAt.IsValue() {
		vals = append(vals, "created_at")
	}
	if s.UpdatedAt.IsValue() {
		vals = append(vals, "updated_at")
	}
	if s.CreatedBy.IsValue() {
		vals = append(vals, "created_by")
	}
	if s.UpdatedBy.IsValue() {
		vals = append(vals, "updated_by")
	}
	return vals
}

func (s TrackLayoutSetter) Overwrite(t *TrackLayout) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.TrackID.IsValue() {
		t.TrackID = s.TrackID.MustGet()
	}
	if s.Name.IsValue() {
		t.Name = s.Name.MustGet()
	}
	if !s.LengthMeters.IsUnset() {
		t.LengthMeters = s.LengthMeters.MustGetNull()
	}
	if !s.LayoutImageURL.IsUnset() {
		t.LayoutImageURL = s.LayoutImageURL.MustGetNull()
	}
	if s.IsActive.IsValue() {
		t.IsActive = s.IsActive.MustGet()
	}
	if s.CreatedAt.IsValue() {
		t.CreatedAt = s.CreatedAt.MustGet()
	}
	if s.UpdatedAt.IsValue() {
		t.UpdatedAt = s.UpdatedAt.MustGet()
	}
	if s.CreatedBy.IsValue() {
		t.CreatedBy = s.CreatedBy.MustGet()
	}
	if s.UpdatedBy.IsValue() {
		t.UpdatedBy = s.UpdatedBy.MustGet()
	}
}

func (s *TrackLayoutSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return TrackLayouts.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	q.AppendValues(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		vals := make([]bob.Expression, 10)
		if s.ID.IsValue() {
			vals[0] = psql.Arg(s.ID.MustGet())
		} else {
			vals[0] = psql.Raw("DEFAULT")
		}

		if s.TrackID.IsValue() {
			vals[1] = psql.Arg(s.TrackID.MustGet())
		} else {
			vals[1] = psql.Raw("DEFAULT")
		}

		if s.Name.IsValue() {
			vals[2] = psql.Arg(s.Name.MustGet())
		} else {
			vals[2] = psql.Raw("DEFAULT")
		}

		if !s.LengthMeters.IsUnset() {
			vals[3] = psql.Arg(s.LengthMeters.MustGetNull())
		} else {
			vals[3] = psql.Raw("DEFAULT")
		}

		if !s.LayoutImageURL.IsUnset() {
			vals[4] = psql.Arg(s.LayoutImageURL.MustGetNull())
		} else {
			vals[4] = psql.Raw("DEFAULT")
		}

		if s.IsActive.IsValue() {
			vals[5] = psql.Arg(s.IsActive.MustGet())
		} else {
			vals[5] = psql.Raw("DEFAULT")
		}

		if s.CreatedAt.IsValue() {
			vals[6] = psql.Arg(s.CreatedAt.MustGet())
		} else {
			vals[6] = psql.Raw("DEFAULT")
		}

		if s.UpdatedAt.IsValue() {
			vals[7] = psql.Arg(s.UpdatedAt.MustGet())
		} else {
			vals[7] = psql.Raw("DEFAULT")
		}

		if s.CreatedBy.IsValue() {
			vals[8] = psql.Arg(s.CreatedBy.MustGet())
		} else {
			vals[8] = psql.Raw("DEFAULT")
		}

		if s.UpdatedBy.IsValue() {
			vals[9] = psql.Arg(s.UpdatedBy.MustGet())
		} else {
			vals[9] = psql.Raw("DEFAULT")
		}

		return bob.ExpressSlice(ctx, w, d, start, vals, "", ", ", "")
	}))
}

func (s TrackLayoutSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s TrackLayoutSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 10)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "id")...),
			psql.Arg(s.ID),
		}})
	}

	if s.TrackID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "track_id")...),
			psql.Arg(s.TrackID),
		}})
	}

	if s.Name.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "name")...),
			psql.Arg(s.Name),
		}})
	}

	if !s.LengthMeters.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "length_meters")...),
			psql.Arg(s.LengthMeters),
		}})
	}

	if !s.LayoutImageURL.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "layout_image_url")...),
			psql.Arg(s.LayoutImageURL),
		}})
	}

	if s.IsActive.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "is_active")...),
			psql.Arg(s.IsActive),
		}})
	}

	if s.CreatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_at")...),
			psql.Arg(s.CreatedAt),
		}})
	}

	if s.UpdatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "updated_at")...),
			psql.Arg(s.UpdatedAt),
		}})
	}

	if s.CreatedBy.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_by")...),
			psql.Arg(s.CreatedBy),
		}})
	}

	if s.UpdatedBy.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "updated_by")...),
			psql.Arg(s.UpdatedBy),
		}})
	}

	return exprs
}

// FindTrackLayout retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindTrackLayout(ctx context.Context, exec bob.Executor, IDPK int32, cols ...string) (*TrackLayout, error) {
	if len(cols) == 0 {
		return TrackLayouts.Query(
			sm.Where(TrackLayouts.Columns.ID.EQ(psql.Arg(IDPK))),
		).One(ctx, exec)
	}

	return TrackLayouts.Query(
		sm.Where(TrackLayouts.Columns.ID.EQ(psql.Arg(IDPK))),
		sm.Columns(TrackLayouts.Columns.Only(cols...)),
	).One(ctx, exec)
}

// TrackLayoutExists checks the presence of a single record by primary key
func TrackLayoutExists(ctx context.Context, exec bob.Executor, IDPK int32) (bool, error) {
	return TrackLayouts.Query(
		sm.Where(TrackLayouts.Columns.ID.EQ(psql.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after TrackLayout is retrieved from the database
func (o *TrackLayout) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = TrackLayouts.AfterSelectHooks.RunHooks(ctx, exec, TrackLayoutSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = TrackLayouts.AfterInsertHooks.RunHooks(ctx, exec, TrackLayoutSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = TrackLayouts.AfterUpdateHooks.RunHooks(ctx, exec, TrackLayoutSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = TrackLayouts.AfterDeleteHooks.RunHooks(ctx, exec, TrackLayoutSlice{o})
	case bob.QueryTypeMerge:
		ctx, err = TrackLayouts.AfterMergeHooks.RunHooks(ctx, exec, TrackLayoutSlice{o})
	}

	return err
}

// primaryKeyVals returns the primary key values of the TrackLayout
func (o *TrackLayout) primaryKeyVals() bob.Expression {
	return psql.Arg(o.ID)
}

func (o *TrackLayout) pkEQ() dialect.Expression {
	return psql.Quote("track_layouts", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the TrackLayout
func (o *TrackLayout) Update(ctx context.Context, exec bob.Executor, s *TrackLayoutSetter) error {
	v, err := TrackLayouts.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single TrackLayout record with an executor
func (o *TrackLayout) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := TrackLayouts.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the TrackLayout using the executor
func (o *TrackLayout) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := TrackLayouts.Query(
		sm.Where(TrackLayouts.Columns.ID.EQ(psql.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after TrackLayoutSlice is retrieved from the database
func (o TrackLayoutSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = TrackLayouts.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = TrackLayouts.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = TrackLayouts.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = TrackLayouts.AfterDeleteHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeMerge:
		ctx, err = TrackLayouts.AfterMergeHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o TrackLayoutSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return psql.Raw("NULL")
	}

	return psql.Quote("track_layouts", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o TrackLayoutSlice) copyMatchingRows(from ...*TrackLayout) {
	for i, old := range o {
		for _, new := range from {
			if new.ID != old.ID {
				continue
			}
			new.R = old.R
			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o TrackLayoutSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return TrackLayouts.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *TrackLayout:
				o.copyMatchingRows(retrieved)
			case []*TrackLayout:
				o.copyMatchingRows(retrieved...)
			case TrackLayoutSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a TrackLayout or a slice of TrackLayout
				// then run the AfterUpdateHooks on the slice
				_, err = TrackLayouts.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o TrackLayoutSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return TrackLayouts.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *TrackLayout:
				o.copyMatchingRows(retrieved)
			case []*TrackLayout:
				o.copyMatchingRows(retrieved...)
			case TrackLayoutSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a TrackLayout or a slice of TrackLayout
				// then run the AfterDeleteHooks on the slice
				_, err = TrackLayouts.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// MergeMod modifies a merge query to run BeforeMergeHooks and AfterMergeHooks
// and updates the slice with the returned rows.
func (o TrackLayoutSlice) MergeMod() bob.Mod[*dialect.MergeQuery] {
	return bob.ModFunc[*dialect.MergeQuery](func(q *dialect.MergeQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return TrackLayouts.BeforeMergeHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *TrackLayout:
				o.copyMatchingRows(retrieved)
			case []*TrackLayout:
				o.copyMatchingRows(retrieved...)
			case TrackLayoutSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a TrackLayout or a slice of TrackLayout
				// then run the AfterMergeHooks on the slice
				_, err = TrackLayouts.AfterMergeHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))
	})
}

func (o TrackLayoutSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals TrackLayoutSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := TrackLayouts.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o TrackLayoutSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := TrackLayouts.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o TrackLayoutSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := TrackLayouts.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// Events starts a query for related objects on events
func (o *TrackLayout) Events(mods ...bob.Mod[*dialect.SelectQuery]) EventsQuery {
	return Events.Query(append(mods,
		sm.Where(Events.Columns.TrackLayoutID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os TrackLayoutSlice) Events(mods ...bob.Mod[*dialect.SelectQuery]) EventsQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	return Events.Query(append(mods,
		sm.Where(psql.Group(Events.Columns.TrackLayoutID).OP("IN", PKArgExpr)),
	)...)
}

// SimulationTrackLayoutAliases starts a query for related objects on simulation_track_layout_aliases
func (o *TrackLayout) SimulationTrackLayoutAliases(mods ...bob.Mod[*dialect.SelectQuery]) SimulationTrackLayoutAliasesQuery {
	return SimulationTrackLayoutAliases.Query(append(mods,
		sm.Where(SimulationTrackLayoutAliases.Columns.TrackLayoutID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os TrackLayoutSlice) SimulationTrackLayoutAliases(mods ...bob.Mod[*dialect.SelectQuery]) SimulationTrackLayoutAliasesQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	return SimulationTrackLayoutAliases.Query(append(mods,
		sm.Where(psql.Group(SimulationTrackLayoutAliases.Columns.TrackLayoutID).OP("IN", PKArgExpr)),
	)...)
}

// Track starts a query for related objects on tracks
func (o *TrackLayout) Track(mods ...bob.Mod[*dialect.SelectQuery]) TracksQuery {
	return Tracks.Query(append(mods,
		sm.Where(Tracks.Columns.ID.EQ(psql.Arg(o.TrackID))),
	)...)
}

func (os TrackLayoutSlice) Track(mods ...bob.Mod[*dialect.SelectQuery]) TracksQuery {
	pkTrackID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkTrackID = append(pkTrackID, o.TrackID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkTrackID), "integer[]")),
	))

	return Tracks.Query(append(mods,
		sm.Where(psql.Group(Tracks.Columns.ID).OP("IN", PKArgExpr)),
	)...)
}

func insertTrackLayoutEvents0(ctx context.Context, exec bob.Executor, events1 []*EventSetter, trackLayout0 *TrackLayout) (EventSlice, error) {
	for i := range events1 {
		events1[i].TrackLayoutID = omit.From(trackLayout0.ID)
	}

	ret, err := Events.Insert(bob.ToMods(events1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertTrackLayoutEvents0: %w", err)
	}

	return ret, nil
}

func attachTrackLayoutEvents0(ctx context.Context, exec bob.Executor, count int, events1 EventSlice, trackLayout0 *TrackLayout) (EventSlice, error) {
	setter := &EventSetter{
		TrackLayoutID: omit.From(trackLayout0.ID),
	}

	err := events1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachTrackLayoutEvents0: %w", err)
	}

	return events1, nil
}

func (trackLayout0 *TrackLayout) InsertEvents(ctx context.Context, exec bob.Executor, related ...*EventSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	events1, err := insertTrackLayoutEvents0(ctx, exec, related, trackLayout0)
	if err != nil {
		return err
	}

	trackLayout0.R.Events = append(trackLayout0.R.Events, events1...)

	for _, rel := range events1 {
		rel.R.TrackLayout = trackLayout0
	}
	return nil
}

func (trackLayout0 *TrackLayout) AttachEvents(ctx context.Context, exec bob.Executor, related ...*Event) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	events1 := EventSlice(related)

	_, err = attachTrackLayoutEvents0(ctx, exec, len(related), events1, trackLayout0)
	if err != nil {
		return err
	}

	trackLayout0.R.Events = append(trackLayout0.R.Events, events1...)

	for _, rel := range related {
		rel.R.TrackLayout = trackLayout0
	}

	return nil
}

func insertTrackLayoutSimulationTrackLayoutAliases0(ctx context.Context, exec bob.Executor, simulationTrackLayoutAliases1 []*SimulationTrackLayoutAliasSetter, trackLayout0 *TrackLayout) (SimulationTrackLayoutAliasSlice, error) {
	for i := range simulationTrackLayoutAliases1 {
		simulationTrackLayoutAliases1[i].TrackLayoutID = omit.From(trackLayout0.ID)
	}

	ret, err := SimulationTrackLayoutAliases.Insert(bob.ToMods(simulationTrackLayoutAliases1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertTrackLayoutSimulationTrackLayoutAliases0: %w", err)
	}

	return ret, nil
}

func attachTrackLayoutSimulationTrackLayoutAliases0(ctx context.Context, exec bob.Executor, count int, simulationTrackLayoutAliases1 SimulationTrackLayoutAliasSlice, trackLayout0 *TrackLayout) (SimulationTrackLayoutAliasSlice, error) {
	setter := &SimulationTrackLayoutAliasSetter{
		TrackLayoutID: omit.From(trackLayout0.ID),
	}

	err := simulationTrackLayoutAliases1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachTrackLayoutSimulationTrackLayoutAliases0: %w", err)
	}

	return simulationTrackLayoutAliases1, nil
}

func (trackLayout0 *TrackLayout) InsertSimulationTrackLayoutAliases(ctx context.Context, exec bob.Executor, related ...*SimulationTrackLayoutAliasSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	simulationTrackLayoutAliases1, err := insertTrackLayoutSimulationTrackLayoutAliases0(ctx, exec, related, trackLayout0)
	if err != nil {
		return err
	}

	trackLayout0.R.SimulationTrackLayoutAliases = append(trackLayout0.R.SimulationTrackLayoutAliases, simulationTrackLayoutAliases1...)

	for _, rel := range simulationTrackLayoutAliases1 {
		rel.R.TrackLayout = trackLayout0
	}
	return nil
}

func (trackLayout0 *TrackLayout) AttachSimulationTrackLayoutAliases(ctx context.Context, exec bob.Executor, related ...*SimulationTrackLayoutAlias) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	simulationTrackLayoutAliases1 := SimulationTrackLayoutAliasSlice(related)

	_, err = attachTrackLayoutSimulationTrackLayoutAliases0(ctx, exec, len(related), simulationTrackLayoutAliases1, trackLayout0)
	if err != nil {
		return err
	}

	trackLayout0.R.SimulationTrackLayoutAliases = append(trackLayout0.R.SimulationTrackLayoutAliases, simulationTrackLayoutAliases1...)

	for _, rel := range related {
		rel.R.TrackLayout = trackLayout0
	}

	return nil
}

func attachTrackLayoutTrack0(ctx context.Context, exec bob.Executor, count int, trackLayout0 *TrackLayout, track1 *Track) (*TrackLayout, error) {
	setter := &TrackLayoutSetter{
		TrackID: omit.From(track1.ID),
	}

	err := trackLayout0.Update(ctx, exec, setter)
	if err != nil {
		return nil, fmt.Errorf("attachTrackLayoutTrack0: %w", err)
	}

	return trackLayout0, nil
}

func (trackLayout0 *TrackLayout) InsertTrack(ctx context.Context, exec bob.Executor, related *TrackSetter) error {
	var err error

	track1, err := Tracks.Insert(related).One(ctx, exec)
	if err != nil {
		return fmt.Errorf("inserting related objects: %w", err)
	}

	_, err = attachTrackLayoutTrack0(ctx, exec, 1, trackLayout0, track1)
	if err != nil {
		return err
	}

	trackLayout0.R.Track = track1

	track1.R.TrackLayouts = append(track1.R.TrackLayouts, trackLayout0)

	return nil
}

func (trackLayout0 *TrackLayout) AttachTrack(ctx context.Context, exec bob.Executor, track1 *Track) error {
	var err error

	_, err = attachTrackLayoutTrack0(ctx, exec, 1, trackLayout0, track1)
	if err != nil {
		return err
	}

	trackLayout0.R.Track = track1

	track1.R.TrackLayouts = append(track1.R.TrackLayouts, trackLayout0)

	return nil
}

type trackLayoutWhere[Q psql.Filterable] struct {
	ID             psql.WhereMod[Q, int32]
	TrackID        psql.WhereMod[Q, int32]
	Name           psql.WhereMod[Q, string]
	LengthMeters   psql.WhereNullMod[Q, int32]
	LayoutImageURL psql.WhereNullMod[Q, string]
	IsActive       psql.WhereMod[Q, bool]
	CreatedAt      psql.WhereMod[Q, time.Time]
	UpdatedAt      psql.WhereMod[Q, time.Time]
	CreatedBy      psql.WhereMod[Q, string]
	UpdatedBy      psql.WhereMod[Q, string]
}

func (trackLayoutWhere[Q]) AliasedAs(alias string) trackLayoutWhere[Q] {
	return buildTrackLayoutWhere[Q](buildTrackLayoutColumns(alias))
}

func buildTrackLayoutWhere[Q psql.Filterable](cols trackLayoutColumns) trackLayoutWhere[Q] {
	return trackLayoutWhere[Q]{
		ID:             psql.Where[Q, int32](cols.ID),
		TrackID:        psql.Where[Q, int32](cols.TrackID),
		Name:           psql.Where[Q, string](cols.Name),
		LengthMeters:   psql.WhereNull[Q, int32](cols.LengthMeters),
		LayoutImageURL: psql.WhereNull[Q, string](cols.LayoutImageURL),
		IsActive:       psql.Where[Q, bool](cols.IsActive),
		CreatedAt:      psql.Where[Q, time.Time](cols.CreatedAt),
		UpdatedAt:      psql.Where[Q, time.Time](cols.UpdatedAt),
		CreatedBy:      psql.Where[Q, string](cols.CreatedBy),
		UpdatedBy:      psql.Where[Q, string](cols.UpdatedBy),
	}
}

func (o *TrackLayout) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Events":
		rels, ok := retrieved.(EventSlice)
		if !ok {
			return fmt.Errorf("trackLayout cannot load %T as %q", retrieved, name)
		}

		o.R.Events = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.TrackLayout = o
			}
		}
		return nil
	case "SimulationTrackLayoutAliases":
		rels, ok := retrieved.(SimulationTrackLayoutAliasSlice)
		if !ok {
			return fmt.Errorf("trackLayout cannot load %T as %q", retrieved, name)
		}

		o.R.SimulationTrackLayoutAliases = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.TrackLayout = o
			}
		}
		return nil
	case "Track":
		rel, ok := retrieved.(*Track)
		if !ok {
			return fmt.Errorf("trackLayout cannot load %T as %q", retrieved, name)
		}

		o.R.Track = rel

		if rel != nil {
			rel.R.TrackLayouts = TrackLayoutSlice{o}
		}
		return nil
	default:
		return fmt.Errorf("trackLayout has no relationship %q", name)
	}
}

type trackLayoutPreloader struct {
	Track func(...psql.PreloadOption) psql.Preloader
}

func buildTrackLayoutPreloader() trackLayoutPreloader {
	return trackLayoutPreloader{
		Track: func(opts ...psql.PreloadOption) psql.Preloader {
			return psql.Preload[*Track, TrackSlice](psql.PreloadRel{
				Name: "Track",
				Sides: []psql.PreloadSide{
					{
						From:        TrackLayouts,
						To:          Tracks,
						FromColumns: []string{"track_id"},
						ToColumns:   []string{"id"},
					},
				},
			}, Tracks.Columns.Names(), opts...)
		},
	}
}

type trackLayoutThenLoader[Q orm.Loadable] struct {
	Events                       func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SimulationTrackLayoutAliases func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Track                        func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildTrackLayoutThenLoader[Q orm.Loadable]() trackLayoutThenLoader[Q] {
	type EventsLoadInterface interface {
		LoadEvents(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SimulationTrackLayoutAliasesLoadInterface interface {
		LoadSimulationTrackLayoutAliases(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type TrackLoadInterface interface {
		LoadTrack(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return trackLayoutThenLoader[Q]{
		Events: thenLoadBuilder[Q](
			"Events",
			func(ctx context.Context, exec bob.Executor, retrieved EventsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadEvents(ctx, exec, mods...)
			},
		),
		SimulationTrackLayoutAliases: thenLoadBuilder[Q](
			"SimulationTrackLayoutAliases",
			func(ctx context.Context, exec bob.Executor, retrieved SimulationTrackLayoutAliasesLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadSimulationTrackLayoutAliases(ctx, exec, mods...)
			},
		),
		Track: thenLoadBuilder[Q](
			"Track",
			func(ctx context.Context, exec bob.Executor, retrieved TrackLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadTrack(ctx, exec, mods...)
			},
		),
	}
}

// LoadEvents loads the trackLayout's Events into the .R struct
func (o *TrackLayout) LoadEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Events = nil

	related, err := o.Events(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.TrackLayout = o
	}

	o.R.Events = related
	return nil
}

// LoadEvents loads the trackLayout's Events into the .R struct
func (os TrackLayoutSlice) LoadEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	events, err := os.Events(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.Events = nil
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range events {

			if !(o.ID == rel.TrackLayoutID) {
				continue
			}

			rel.R.TrackLayout = o

			o.R.Events = append(o.R.Events, rel)
		}
	}

	return nil
}

// LoadSimulationTrackLayoutAliases loads the trackLayout's SimulationTrackLayoutAliases into the .R struct
func (o *TrackLayout) LoadSimulationTrackLayoutAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.SimulationTrackLayoutAliases = nil

	related, err := o.SimulationTrackLayoutAliases(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.TrackLayout = o
	}

	o.R.SimulationTrackLayoutAliases = related
	return nil
}

// LoadSimulationTrackLayoutAliases loads the trackLayout's SimulationTrackLayoutAliases into the .R struct
func (os TrackLayoutSlice) LoadSimulationTrackLayoutAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	simulationTrackLayoutAliases, err := os.SimulationTrackLayoutAliases(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.SimulationTrackLayoutAliases = nil
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range simulationTrackLayoutAliases {

			if !(o.ID == rel.TrackLayoutID) {
				continue
			}

			rel.R.TrackLayout = o

			o.R.SimulationTrackLayoutAliases = append(o.R.SimulationTrackLayoutAliases, rel)
		}
	}

	return nil
}

// LoadTrack loads the trackLayout's Track into the .R struct
func (o *TrackLayout) LoadTrack(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Track = nil

	related, err := o.Track(mods...).One(ctx, exec)
	if err != nil {
		return err
	}

	related.R.TrackLayouts = TrackLayoutSlice{o}

	o.R.Track = related
	return nil
}

// LoadTrack loads the trackLayout's Track into the .R struct
func (os TrackLayoutSlice) LoadTrack(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	tracks, err := os.Track(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		for _, rel := range tracks {

			if !(o.TrackID == rel.ID) {
				continue
			}

			rel.R.TrackLayouts = append(rel.R.TrackLayouts, o)

			o.R.Track = rel
			break
		}
	}

	return nil
}

// trackLayoutC is where relationship counts are stored.
type trackLayoutC struct {
	Events                       *int64
	SimulationTrackLayoutAliases *int64
}

// PreloadCount sets a count in the C struct by name
func (o *TrackLayout) PreloadCount(name string, count int64) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Events":
		o.C.Events = &count
	case "SimulationTrackLayoutAliases":
		o.C.SimulationTrackLayoutAliases = &count
	}
	return nil
}

type trackLayoutCountPreloader struct {
	Events                       func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	SimulationTrackLayoutAliases func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
}

func buildTrackLayoutCountPreloader() trackLayoutCountPreloader {
	return trackLayoutCountPreloader{
		Events: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*TrackLayout]("Events", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = TrackLayouts.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(Events.Name()),
					sm.Where(psql.Quote(Events.Alias(), "track_layout_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		SimulationTrackLayoutAliases: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*TrackLayout]("SimulationTrackLayoutAliases", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = TrackLayouts.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(SimulationTrackLayoutAliases.Name()),
					sm.Where(psql.Quote(SimulationTrackLayoutAliases.Alias(), "track_layout_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
	}
}

type trackLayoutCountThenLoader[Q orm.Loadable] struct {
	Events                       func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SimulationTrackLayoutAliases func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildTrackLayoutCountThenLoader[Q orm.Loadable]() trackLayoutCountThenLoader[Q] {
	type EventsCountInterface interface {
		LoadCountEvents(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SimulationTrackLayoutAliasesCountInterface interface {
		LoadCountSimulationTrackLayoutAliases(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return trackLayoutCountThenLoader[Q]{
		Events: countThenLoadBuilder[Q](
			"Events",
			func(ctx context.Context, exec bob.Executor, retrieved EventsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountEvents(ctx, exec, mods...)
			},
		),
		SimulationTrackLayoutAliases: countThenLoadBuilder[Q](
			"SimulationTrackLayoutAliases",
			func(ctx context.Context, exec bob.Executor, retrieved SimulationTrackLayoutAliasesCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountSimulationTrackLayoutAliases(ctx, exec, mods...)
			},
		),
	}
}

// LoadCountEvents loads the count of Events into the C struct
func (o *TrackLayout) LoadCountEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.Events(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.Events = &count
	return nil
}

// LoadCountEvents loads the count of Events for a slice in a single batch query
func (os TrackLayoutSlice) LoadCountEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			Events.Columns.TrackLayoutID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(Events.NameAs()),

		// WHERE fk IN (parent PKs)
		sm.Where(Events.Columns.TrackLayoutID.OP("IN", PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(Events.Columns.TrackLayoutID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.Events = &count
	}

	return nil
}

// LoadCountSimulationTrackLayoutAliases loads the count of SimulationTrackLayoutAliases into the C struct
func (o *TrackLayout) LoadCountSimulationTrackLayoutAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.SimulationTrackLayoutAliases(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.SimulationTrackLayoutAliases = &count
	return nil
}

// LoadCountSimulationTrackLayoutAliases loads the count of SimulationTrackLayoutAliases for a slice in a single batch query
func (os TrackLayoutSlice) LoadCountSimulationTrackLayoutAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Select(sm.Columns(
		psql.F("unnest", psql.Cast(psql.Arg(pkID), "integer[]")),
	))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			SimulationTrackLayoutAliases.Columns.TrackLayoutID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(SimulationTrackLayoutAliases.NameAs()),

		// WHERE fk IN (parent PKs)
		sm.Where(SimulationTrackLayoutAliases.Columns.TrackLayoutID.OP("IN", PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(SimulationTrackLayoutAliases.Columns.TrackLayoutID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.SimulationTrackLayoutAliases = &count
	}

	return nil
}

type trackLayoutJoins[Q dialect.Joinable] struct {
	typ                          string
	Events                       modAs[Q, eventColumns]
	SimulationTrackLayoutAliases modAs[Q, simulationTrackLayoutAliasColumns]
	Track                        modAs[Q, trackColumns]
}

func (j trackLayoutJoins[Q]) aliasedAs(alias string) trackLayoutJoins[Q] {
	return buildTrackLayoutJoins[Q](buildTrackLayoutColumns(alias), j.typ)
}

func buildTrackLayoutJoins[Q dialect.Joinable](cols trackLayoutColumns, typ string) trackLayoutJoins[Q] {
	return trackLayoutJoins[Q]{
		typ: typ,
		Events: modAs[Q, eventColumns]{
			c: Events.Columns,
			f: func(to eventColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Events.Name().As(to.Alias())).On(
						to.TrackLayoutID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		SimulationTrackLayoutAliases: modAs[Q, simulationTrackLayoutAliasColumns]{
			c: SimulationTrackLayoutAliases.Columns,
			f: func(to simulationTrackLayoutAliasColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, SimulationTrackLayoutAliases.Name().As(to.Alias())).On(
						to.TrackLayoutID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		Track: modAs[Q, trackColumns]{
			c: Tracks.Columns,
			f: func(to trackColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Tracks.Name().As(to.Alias())).On(
						to.ID.EQ(cols.TrackID),
					))
				}

				return mods
			},
		},
	}
}
