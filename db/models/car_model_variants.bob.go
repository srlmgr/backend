// Code generated by BobGen psql v0.48.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"fmt"
	"io"
	"time"

	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/dialect"
	"github.com/stephenafamo/bob/dialect/psql/dm"
	"github.com/stephenafamo/bob/dialect/psql/sm"
	"github.com/stephenafamo/bob/dialect/psql/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
	"github.com/stephenafamo/bob/types/pgtypes"
	"github.com/stephenafamo/scan"
)

// CarModelVariant is an object representing the database table.
type CarModelVariant struct {
	ID         int32     `db:"id,pk" `
	CarModelID int32     `db:"car_model_id" `
	Name       string    `db:"name" `
	IsActive   bool      `db:"is_active" `
	CreatedAt  time.Time `db:"created_at" `
	UpdatedAt  time.Time `db:"updated_at" `
	CreatedBy  string    `db:"created_by" `
	UpdatedBy  string    `db:"updated_by" `

	R carModelVariantR `db:"-" `

	C carModelVariantC `db:"-" `
}

// CarModelVariantSlice is an alias for a slice of pointers to CarModelVariant.
// This should almost always be used instead of []*CarModelVariant.
type CarModelVariantSlice []*CarModelVariant

// CarModelVariants contains methods to work with the car_model_variants table
var CarModelVariants = psql.NewTablex[*CarModelVariant, CarModelVariantSlice, *CarModelVariantSetter]("", "car_model_variants", buildCarModelVariantColumns("car_model_variants"))

// CarModelVariantsQuery is a query on the car_model_variants table
type CarModelVariantsQuery = *psql.ViewQuery[*CarModelVariant, CarModelVariantSlice]

// carModelVariantR is where relationships are stored.
type carModelVariantR struct {
	CarClassesToCarModels  CarClassesToCarModelSlice  // car_classes_to_car_models.car_classes_to_car_models_car_model_variant_id_fk
	CarModel               *CarModel                  // car_model_variants.car_model_variants_car_model_id_fk
	ResultEntries          ResultEntrySlice           // result_entries.result_entries_car_model_variant_id_fk
	SeasonCarModelVariants SeasonCarModelVariantSlice // season_car_model_variants.season_car_model_variants_car_model_variant_id_fk
	SeasonDrivers          SeasonDriverSlice          // season_drivers.season_drivers_car_model_variant_id_fk
	SimulationCarAliases   SimulationCarAliasSlice    // simulation_car_aliases.simulation_car_aliases_car_model_variant_id_fk
	Teams                  TeamSlice                  // teams.teams_car_model_variant_id_fk
	// Loaded reports whether each relationship has been loaded.
	// A relationship's bool is set by Load*, Preload, ThenLoad, factory builds,
	// and to-one Attach/Insert operations. To-many Attach/Insert operations leave it unchanged.
	Loaded carModelVariantRLoaded `db:"-" `
}

// carModelVariantRLoaded tracks which relationships on CarModelVariant have been loaded.
type carModelVariantRLoaded struct {
	CarClassesToCarModels  bool // car_classes_to_car_models.car_classes_to_car_models_car_model_variant_id_fk
	CarModel               bool // car_model_variants.car_model_variants_car_model_id_fk
	ResultEntries          bool // result_entries.result_entries_car_model_variant_id_fk
	SeasonCarModelVariants bool // season_car_model_variants.season_car_model_variants_car_model_variant_id_fk
	SeasonDrivers          bool // season_drivers.season_drivers_car_model_variant_id_fk
	SimulationCarAliases   bool // simulation_car_aliases.simulation_car_aliases_car_model_variant_id_fk
	Teams                  bool // teams.teams_car_model_variant_id_fk
}

func buildCarModelVariantColumns(tableName string) carModelVariantColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "car_model_id", "name", "is_active", "created_at", "updated_at", "created_by", "updated_by",
	)

	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}

	return carModelVariantColumns{
		ColumnsExpr: columnsExpr,
		tableAlias:  tableName,
		ID:          buildCarModelVariantColumn(tableName, "id"),
		CarModelID:  buildCarModelVariantColumn(tableName, "car_model_id"),
		Name:        buildCarModelVariantColumn(tableName, "name"),
		IsActive:    buildCarModelVariantColumn(tableName, "is_active"),
		CreatedAt:   buildCarModelVariantColumn(tableName, "created_at"),
		UpdatedAt:   buildCarModelVariantColumn(tableName, "updated_at"),
		CreatedBy:   buildCarModelVariantColumn(tableName, "created_by"),
		UpdatedBy:   buildCarModelVariantColumn(tableName, "updated_by"),
	}
}

type carModelVariantColumns struct {
	expr.ColumnsExpr
	tableAlias string
	ID         carModelVariantColumn
	CarModelID carModelVariantColumn
	Name       carModelVariantColumn
	IsActive   carModelVariantColumn
	CreatedAt  carModelVariantColumn
	UpdatedAt  carModelVariantColumn
	CreatedBy  carModelVariantColumn
	UpdatedBy  carModelVariantColumn
}

// Alias returns the current table alias for the columns set.
func (c carModelVariantColumns) Alias() string {
	return c.tableAlias
}

// AliasedAs returns a copy of the columns set qualified by tableName.
func (carModelVariantColumns) AliasedAs(tableName string) carModelVariantColumns {
	return buildCarModelVariantColumns(tableName)
}

// Unqualified returns a copy of the columns set without table qualification.
func (c carModelVariantColumns) Unqualified() carModelVariantColumns {
	return buildCarModelVariantColumns("")
}

func buildCarModelVariantColumn(alias, name string) carModelVariantColumn {
	return carModelVariantColumn{
		Expression: psql.Quote(alias, name),
		alias:      alias,
		name:       name,
	}
}

type carModelVariantColumn struct {
	psql.Expression
	alias string
	name  string
}

// Name returns the unqualified column name.
func (c carModelVariantColumn) Name() string {
	return c.name
}

// ShouldOmitParens prevents automatic parenthesis wrapping in expression builders.
func (c carModelVariantColumn) ShouldOmitParens() bool {
	return true
}

// CarModelVariantSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type CarModelVariantSetter struct {
	ID         omit.Val[int32]     `db:"id,pk" `
	CarModelID omit.Val[int32]     `db:"car_model_id" `
	Name       omit.Val[string]    `db:"name" `
	IsActive   omit.Val[bool]      `db:"is_active" `
	CreatedAt  omit.Val[time.Time] `db:"created_at" `
	UpdatedAt  omit.Val[time.Time] `db:"updated_at" `
	CreatedBy  omit.Val[string]    `db:"created_by" `
	UpdatedBy  omit.Val[string]    `db:"updated_by" `
}

func (s CarModelVariantSetter) SetColumns() []string {
	vals := make([]string, 0, 8)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.CarModelID.IsValue() {
		vals = append(vals, "car_model_id")
	}
	if s.Name.IsValue() {
		vals = append(vals, "name")
	}
	if s.IsActive.IsValue() {
		vals = append(vals, "is_active")
	}
	if s.CreatedAt.IsValue() {
		vals = append(vals, "created_at")
	}
	if s.UpdatedAt.IsValue() {
		vals = append(vals, "updated_at")
	}
	if s.CreatedBy.IsValue() {
		vals = append(vals, "created_by")
	}
	if s.UpdatedBy.IsValue() {
		vals = append(vals, "updated_by")
	}
	return vals
}

func (s CarModelVariantSetter) Overwrite(t *CarModelVariant) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.CarModelID.IsValue() {
		t.CarModelID = s.CarModelID.MustGet()
	}
	if s.Name.IsValue() {
		t.Name = s.Name.MustGet()
	}
	if s.IsActive.IsValue() {
		t.IsActive = s.IsActive.MustGet()
	}
	if s.CreatedAt.IsValue() {
		t.CreatedAt = s.CreatedAt.MustGet()
	}
	if s.UpdatedAt.IsValue() {
		t.UpdatedAt = s.UpdatedAt.MustGet()
	}
	if s.CreatedBy.IsValue() {
		t.CreatedBy = s.CreatedBy.MustGet()
	}
	if s.UpdatedBy.IsValue() {
		t.UpdatedBy = s.UpdatedBy.MustGet()
	}
}

func (s *CarModelVariantSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return CarModelVariants.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	q.AppendValues(
		bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.ID.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.ID.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.CarModelID.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.CarModelID.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.Name.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.Name.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.IsActive.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.IsActive.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.CreatedAt.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.CreatedAt.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.UpdatedAt.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.UpdatedAt.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.CreatedBy.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.CreatedBy.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.UpdatedBy.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.UpdatedBy.MustGet()).WriteSQL(ctx, w, d, start)
		}))
}

func (s CarModelVariantSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s CarModelVariantSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 8)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "id")...),
			psql.Arg(s.ID),
		}})
	}

	if s.CarModelID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "car_model_id")...),
			psql.Arg(s.CarModelID),
		}})
	}

	if s.Name.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "name")...),
			psql.Arg(s.Name),
		}})
	}

	if s.IsActive.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "is_active")...),
			psql.Arg(s.IsActive),
		}})
	}

	if s.CreatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_at")...),
			psql.Arg(s.CreatedAt),
		}})
	}

	if s.UpdatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "updated_at")...),
			psql.Arg(s.UpdatedAt),
		}})
	}

	if s.CreatedBy.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_by")...),
			psql.Arg(s.CreatedBy),
		}})
	}

	if s.UpdatedBy.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "updated_by")...),
			psql.Arg(s.UpdatedBy),
		}})
	}

	return exprs
}

// FindCarModelVariant retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindCarModelVariant(ctx context.Context, exec bob.Executor, IDPK int32, cols ...string) (*CarModelVariant, error) {
	if len(cols) == 0 {
		return CarModelVariants.Query(
			sm.Where(CarModelVariants.Columns.ID.EQ(psql.Arg(IDPK))),
		).One(ctx, exec)
	}

	return CarModelVariants.Query(
		sm.Where(CarModelVariants.Columns.ID.EQ(psql.Arg(IDPK))),
		sm.Columns(CarModelVariants.Columns.Only(cols...)),
	).One(ctx, exec)
}

// CarModelVariantExists checks the presence of a single record by primary key
func CarModelVariantExists(ctx context.Context, exec bob.Executor, IDPK int32) (bool, error) {
	return CarModelVariants.Query(
		sm.Where(CarModelVariants.Columns.ID.EQ(psql.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after CarModelVariant is retrieved from the database
func (o *CarModelVariant) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = CarModelVariants.AfterSelectHooks.RunHooks(ctx, exec, CarModelVariantSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = CarModelVariants.AfterInsertHooks.RunHooks(ctx, exec, CarModelVariantSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = CarModelVariants.AfterUpdateHooks.RunHooks(ctx, exec, CarModelVariantSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = CarModelVariants.AfterDeleteHooks.RunHooks(ctx, exec, CarModelVariantSlice{o})
	case bob.QueryTypeMerge:
		ctx, err = CarModelVariants.AfterMergeHooks.RunHooks(ctx, exec, CarModelVariantSlice{o})
	}

	return err
}

// primaryKeyVals returns the primary key values of the CarModelVariant
func (o *CarModelVariant) primaryKeyVals() bob.Expression {
	return psql.Arg(o.ID)
}

func (o *CarModelVariant) pkEQ() dialect.Expression {
	return psql.Quote("car_model_variants", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the CarModelVariant
func (o *CarModelVariant) Update(ctx context.Context, exec bob.Executor, s *CarModelVariantSetter) error {
	v, err := CarModelVariants.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single CarModelVariant record with an executor
func (o *CarModelVariant) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := CarModelVariants.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the CarModelVariant using the executor
func (o *CarModelVariant) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := CarModelVariants.Query(
		sm.Where(CarModelVariants.Columns.ID.EQ(psql.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after CarModelVariantSlice is retrieved from the database
func (o CarModelVariantSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = CarModelVariants.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = CarModelVariants.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = CarModelVariants.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = CarModelVariants.AfterDeleteHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeMerge:
		ctx, err = CarModelVariants.AfterMergeHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o CarModelVariantSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return psql.Raw("NULL")
	}

	return psql.Quote("car_model_variants", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o CarModelVariantSlice) copyMatchingRows(from ...*CarModelVariant) {
	for i, old := range o {
		for _, new := range from {
			if new.ID != old.ID {
				continue
			}
			new.R = old.R
			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o CarModelVariantSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return CarModelVariants.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *CarModelVariant:
				o.copyMatchingRows(retrieved)
			case []*CarModelVariant:
				o.copyMatchingRows(retrieved...)
			case CarModelVariantSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a CarModelVariant or a slice of CarModelVariant
				// then run the AfterUpdateHooks on the slice
				_, err = CarModelVariants.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o CarModelVariantSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return CarModelVariants.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *CarModelVariant:
				o.copyMatchingRows(retrieved)
			case []*CarModelVariant:
				o.copyMatchingRows(retrieved...)
			case CarModelVariantSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a CarModelVariant or a slice of CarModelVariant
				// then run the AfterDeleteHooks on the slice
				_, err = CarModelVariants.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// MergeMod modifies a merge query to run BeforeMergeHooks and AfterMergeHooks
// and updates the slice with the returned rows.
func (o CarModelVariantSlice) MergeMod() bob.Mod[*dialect.MergeQuery] {
	return bob.ModFunc[*dialect.MergeQuery](func(q *dialect.MergeQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return CarModelVariants.BeforeMergeHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *CarModelVariant:
				o.copyMatchingRows(retrieved)
			case []*CarModelVariant:
				o.copyMatchingRows(retrieved...)
			case CarModelVariantSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a CarModelVariant or a slice of CarModelVariant
				// then run the AfterMergeHooks on the slice
				_, err = CarModelVariants.AfterMergeHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))
	})
}

func (o CarModelVariantSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals CarModelVariantSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := CarModelVariants.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o CarModelVariantSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := CarModelVariants.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o CarModelVariantSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := CarModelVariants.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// CarClassesToCarModels starts a query for related objects on car_classes_to_car_models
func (o *CarModelVariant) CarClassesToCarModels(mods ...bob.Mod[*dialect.SelectQuery]) CarClassesToCarModelsQuery {
	return CarClassesToCarModels.Query(append(mods,
		sm.Where(CarClassesToCarModels.Columns.CarModelVariantID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os CarModelVariantSlice) CarClassesToCarModels(mods ...bob.Mod[*dialect.SelectQuery]) CarClassesToCarModelsQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return CarClassesToCarModels.Query(append(mods,
		sm.Where(CarClassesToCarModels.Columns.CarModelVariantID.EQ(PKArgExpr)),
	)...)
}

// CarModel starts a query for related objects on car_models
func (o *CarModelVariant) CarModel(mods ...bob.Mod[*dialect.SelectQuery]) CarModelsQuery {
	return CarModels.Query(append(mods,
		sm.Where(CarModels.Columns.ID.EQ(psql.Arg(o.CarModelID))),
	)...)
}

func (os CarModelVariantSlice) CarModel(mods ...bob.Mod[*dialect.SelectQuery]) CarModelsQuery {
	pkCarModelID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkCarModelID = append(pkCarModelID, o.CarModelID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkCarModelID), "integer[]"))

	return CarModels.Query(append(mods,
		sm.Where(CarModels.Columns.ID.EQ(PKArgExpr)),
	)...)
}

// ResultEntries starts a query for related objects on result_entries
func (o *CarModelVariant) ResultEntries(mods ...bob.Mod[*dialect.SelectQuery]) ResultEntriesQuery {
	return ResultEntries.Query(append(mods,
		sm.Where(ResultEntries.Columns.CarModelVariantID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os CarModelVariantSlice) ResultEntries(mods ...bob.Mod[*dialect.SelectQuery]) ResultEntriesQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return ResultEntries.Query(append(mods,
		sm.Where(ResultEntries.Columns.CarModelVariantID.EQ(PKArgExpr)),
	)...)
}

// SeasonCarModelVariants starts a query for related objects on season_car_model_variants
func (o *CarModelVariant) SeasonCarModelVariants(mods ...bob.Mod[*dialect.SelectQuery]) SeasonCarModelVariantsQuery {
	return SeasonCarModelVariants.Query(append(mods,
		sm.Where(SeasonCarModelVariants.Columns.CarModelVariantID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os CarModelVariantSlice) SeasonCarModelVariants(mods ...bob.Mod[*dialect.SelectQuery]) SeasonCarModelVariantsQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return SeasonCarModelVariants.Query(append(mods,
		sm.Where(SeasonCarModelVariants.Columns.CarModelVariantID.EQ(PKArgExpr)),
	)...)
}

// SeasonDrivers starts a query for related objects on season_drivers
func (o *CarModelVariant) SeasonDrivers(mods ...bob.Mod[*dialect.SelectQuery]) SeasonDriversQuery {
	return SeasonDrivers.Query(append(mods,
		sm.Where(SeasonDrivers.Columns.CarModelVariantID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os CarModelVariantSlice) SeasonDrivers(mods ...bob.Mod[*dialect.SelectQuery]) SeasonDriversQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return SeasonDrivers.Query(append(mods,
		sm.Where(SeasonDrivers.Columns.CarModelVariantID.EQ(PKArgExpr)),
	)...)
}

// SimulationCarAliases starts a query for related objects on simulation_car_aliases
func (o *CarModelVariant) SimulationCarAliases(mods ...bob.Mod[*dialect.SelectQuery]) SimulationCarAliasesQuery {
	return SimulationCarAliases.Query(append(mods,
		sm.Where(SimulationCarAliases.Columns.CarModelVariantID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os CarModelVariantSlice) SimulationCarAliases(mods ...bob.Mod[*dialect.SelectQuery]) SimulationCarAliasesQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return SimulationCarAliases.Query(append(mods,
		sm.Where(SimulationCarAliases.Columns.CarModelVariantID.EQ(PKArgExpr)),
	)...)
}

// Teams starts a query for related objects on teams
func (o *CarModelVariant) Teams(mods ...bob.Mod[*dialect.SelectQuery]) TeamsQuery {
	return Teams.Query(append(mods,
		sm.Where(Teams.Columns.CarModelVariantID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os CarModelVariantSlice) Teams(mods ...bob.Mod[*dialect.SelectQuery]) TeamsQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return Teams.Query(append(mods,
		sm.Where(Teams.Columns.CarModelVariantID.EQ(PKArgExpr)),
	)...)
}

func insertCarModelVariantCarClassesToCarModels0(ctx context.Context, exec bob.Executor, carClassesToCarModels1 []*CarClassesToCarModelSetter, carModelVariant0 *CarModelVariant) (CarClassesToCarModelSlice, error) {
	for i := range carClassesToCarModels1 {
		carClassesToCarModels1[i].CarModelVariantID = omitnull.From(carModelVariant0.ID)
	}

	ret, err := CarClassesToCarModels.Insert(bob.ToMods(carClassesToCarModels1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertCarModelVariantCarClassesToCarModels0: %w", err)
	}

	return ret, nil
}

func attachCarModelVariantCarClassesToCarModels0(ctx context.Context, exec bob.Executor, count int, carClassesToCarModels1 CarClassesToCarModelSlice, carModelVariant0 *CarModelVariant) (CarClassesToCarModelSlice, error) {
	setter := &CarClassesToCarModelSetter{
		CarModelVariantID: omitnull.From(carModelVariant0.ID),
	}

	err := carClassesToCarModels1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantCarClassesToCarModels0: %w", err)
	}

	return carClassesToCarModels1, nil
}

func (carModelVariant0 *CarModelVariant) InsertCarClassesToCarModels(ctx context.Context, exec bob.Executor, related ...*CarClassesToCarModelSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	carClassesToCarModels1, err := insertCarModelVariantCarClassesToCarModels0(ctx, exec, related, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.CarClassesToCarModels = append(carModelVariant0.R.CarClassesToCarModels, carClassesToCarModels1...)

	for _, rel := range carClassesToCarModels1 {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}
	return nil
}

func (carModelVariant0 *CarModelVariant) AttachCarClassesToCarModels(ctx context.Context, exec bob.Executor, related ...*CarClassesToCarModel) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	carClassesToCarModels1 := CarClassesToCarModelSlice(related)

	_, err = attachCarModelVariantCarClassesToCarModels0(ctx, exec, len(related), carClassesToCarModels1, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.CarClassesToCarModels = append(carModelVariant0.R.CarClassesToCarModels, carClassesToCarModels1...)

	for _, rel := range related {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}

	return nil
}

func attachCarModelVariantCarModel0(ctx context.Context, exec bob.Executor, count int, carModelVariant0 *CarModelVariant, carModel1 *CarModel) (*CarModelVariant, error) {
	setter := &CarModelVariantSetter{
		CarModelID: omit.From(carModel1.ID),
	}

	err := carModelVariant0.Update(ctx, exec, setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantCarModel0: %w", err)
	}

	return carModelVariant0, nil
}

func (carModelVariant0 *CarModelVariant) InsertCarModel(ctx context.Context, exec bob.Executor, related *CarModelSetter) error {
	var err error

	carModel1, err := CarModels.Insert(related).One(ctx, exec)
	if err != nil {
		return fmt.Errorf("inserting related objects: %w", err)
	}

	_, err = attachCarModelVariantCarModel0(ctx, exec, 1, carModelVariant0, carModel1)
	if err != nil {
		return err
	}

	carModelVariant0.R.CarModel = carModel1
	carModelVariant0.R.Loaded.CarModel = true

	carModel1.R.CarModelVariants = append(carModel1.R.CarModelVariants, carModelVariant0)

	return nil
}

func (carModelVariant0 *CarModelVariant) AttachCarModel(ctx context.Context, exec bob.Executor, carModel1 *CarModel) error {
	var err error

	_, err = attachCarModelVariantCarModel0(ctx, exec, 1, carModelVariant0, carModel1)
	if err != nil {
		return err
	}

	carModelVariant0.R.CarModel = carModel1
	carModelVariant0.R.Loaded.CarModel = true

	carModel1.R.CarModelVariants = append(carModel1.R.CarModelVariants, carModelVariant0)

	return nil
}

func insertCarModelVariantResultEntries0(ctx context.Context, exec bob.Executor, resultEntries1 []*ResultEntrySetter, carModelVariant0 *CarModelVariant) (ResultEntrySlice, error) {
	for i := range resultEntries1 {
		resultEntries1[i].CarModelVariantID = omitnull.From(carModelVariant0.ID)
	}

	ret, err := ResultEntries.Insert(bob.ToMods(resultEntries1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertCarModelVariantResultEntries0: %w", err)
	}

	return ret, nil
}

func attachCarModelVariantResultEntries0(ctx context.Context, exec bob.Executor, count int, resultEntries1 ResultEntrySlice, carModelVariant0 *CarModelVariant) (ResultEntrySlice, error) {
	setter := &ResultEntrySetter{
		CarModelVariantID: omitnull.From(carModelVariant0.ID),
	}

	err := resultEntries1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantResultEntries0: %w", err)
	}

	return resultEntries1, nil
}

func (carModelVariant0 *CarModelVariant) InsertResultEntries(ctx context.Context, exec bob.Executor, related ...*ResultEntrySetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	resultEntries1, err := insertCarModelVariantResultEntries0(ctx, exec, related, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.ResultEntries = append(carModelVariant0.R.ResultEntries, resultEntries1...)

	for _, rel := range resultEntries1 {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}
	return nil
}

func (carModelVariant0 *CarModelVariant) AttachResultEntries(ctx context.Context, exec bob.Executor, related ...*ResultEntry) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	resultEntries1 := ResultEntrySlice(related)

	_, err = attachCarModelVariantResultEntries0(ctx, exec, len(related), resultEntries1, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.ResultEntries = append(carModelVariant0.R.ResultEntries, resultEntries1...)

	for _, rel := range related {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}

	return nil
}

func insertCarModelVariantSeasonCarModelVariants0(ctx context.Context, exec bob.Executor, seasonCarModelVariants1 []*SeasonCarModelVariantSetter, carModelVariant0 *CarModelVariant) (SeasonCarModelVariantSlice, error) {
	for i := range seasonCarModelVariants1 {
		seasonCarModelVariants1[i].CarModelVariantID = omit.From(carModelVariant0.ID)
	}

	ret, err := SeasonCarModelVariants.Insert(bob.ToMods(seasonCarModelVariants1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertCarModelVariantSeasonCarModelVariants0: %w", err)
	}

	return ret, nil
}

func attachCarModelVariantSeasonCarModelVariants0(ctx context.Context, exec bob.Executor, count int, seasonCarModelVariants1 SeasonCarModelVariantSlice, carModelVariant0 *CarModelVariant) (SeasonCarModelVariantSlice, error) {
	setter := &SeasonCarModelVariantSetter{
		CarModelVariantID: omit.From(carModelVariant0.ID),
	}

	err := seasonCarModelVariants1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantSeasonCarModelVariants0: %w", err)
	}

	return seasonCarModelVariants1, nil
}

func (carModelVariant0 *CarModelVariant) InsertSeasonCarModelVariants(ctx context.Context, exec bob.Executor, related ...*SeasonCarModelVariantSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	seasonCarModelVariants1, err := insertCarModelVariantSeasonCarModelVariants0(ctx, exec, related, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.SeasonCarModelVariants = append(carModelVariant0.R.SeasonCarModelVariants, seasonCarModelVariants1...)

	for _, rel := range seasonCarModelVariants1 {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}
	return nil
}

func (carModelVariant0 *CarModelVariant) AttachSeasonCarModelVariants(ctx context.Context, exec bob.Executor, related ...*SeasonCarModelVariant) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	seasonCarModelVariants1 := SeasonCarModelVariantSlice(related)

	_, err = attachCarModelVariantSeasonCarModelVariants0(ctx, exec, len(related), seasonCarModelVariants1, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.SeasonCarModelVariants = append(carModelVariant0.R.SeasonCarModelVariants, seasonCarModelVariants1...)

	for _, rel := range related {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}

	return nil
}

func insertCarModelVariantSeasonDrivers0(ctx context.Context, exec bob.Executor, seasonDrivers1 []*SeasonDriverSetter, carModelVariant0 *CarModelVariant) (SeasonDriverSlice, error) {
	for i := range seasonDrivers1 {
		seasonDrivers1[i].CarModelVariantID = omit.From(carModelVariant0.ID)
	}

	ret, err := SeasonDrivers.Insert(bob.ToMods(seasonDrivers1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertCarModelVariantSeasonDrivers0: %w", err)
	}

	return ret, nil
}

func attachCarModelVariantSeasonDrivers0(ctx context.Context, exec bob.Executor, count int, seasonDrivers1 SeasonDriverSlice, carModelVariant0 *CarModelVariant) (SeasonDriverSlice, error) {
	setter := &SeasonDriverSetter{
		CarModelVariantID: omit.From(carModelVariant0.ID),
	}

	err := seasonDrivers1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantSeasonDrivers0: %w", err)
	}

	return seasonDrivers1, nil
}

func (carModelVariant0 *CarModelVariant) InsertSeasonDrivers(ctx context.Context, exec bob.Executor, related ...*SeasonDriverSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	seasonDrivers1, err := insertCarModelVariantSeasonDrivers0(ctx, exec, related, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.SeasonDrivers = append(carModelVariant0.R.SeasonDrivers, seasonDrivers1...)

	for _, rel := range seasonDrivers1 {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}
	return nil
}

func (carModelVariant0 *CarModelVariant) AttachSeasonDrivers(ctx context.Context, exec bob.Executor, related ...*SeasonDriver) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	seasonDrivers1 := SeasonDriverSlice(related)

	_, err = attachCarModelVariantSeasonDrivers0(ctx, exec, len(related), seasonDrivers1, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.SeasonDrivers = append(carModelVariant0.R.SeasonDrivers, seasonDrivers1...)

	for _, rel := range related {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}

	return nil
}

func insertCarModelVariantSimulationCarAliases0(ctx context.Context, exec bob.Executor, simulationCarAliases1 []*SimulationCarAliasSetter, carModelVariant0 *CarModelVariant) (SimulationCarAliasSlice, error) {
	for i := range simulationCarAliases1 {
		simulationCarAliases1[i].CarModelVariantID = omit.From(carModelVariant0.ID)
	}

	ret, err := SimulationCarAliases.Insert(bob.ToMods(simulationCarAliases1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertCarModelVariantSimulationCarAliases0: %w", err)
	}

	return ret, nil
}

func attachCarModelVariantSimulationCarAliases0(ctx context.Context, exec bob.Executor, count int, simulationCarAliases1 SimulationCarAliasSlice, carModelVariant0 *CarModelVariant) (SimulationCarAliasSlice, error) {
	setter := &SimulationCarAliasSetter{
		CarModelVariantID: omit.From(carModelVariant0.ID),
	}

	err := simulationCarAliases1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantSimulationCarAliases0: %w", err)
	}

	return simulationCarAliases1, nil
}

func (carModelVariant0 *CarModelVariant) InsertSimulationCarAliases(ctx context.Context, exec bob.Executor, related ...*SimulationCarAliasSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	simulationCarAliases1, err := insertCarModelVariantSimulationCarAliases0(ctx, exec, related, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.SimulationCarAliases = append(carModelVariant0.R.SimulationCarAliases, simulationCarAliases1...)

	for _, rel := range simulationCarAliases1 {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}
	return nil
}

func (carModelVariant0 *CarModelVariant) AttachSimulationCarAliases(ctx context.Context, exec bob.Executor, related ...*SimulationCarAlias) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	simulationCarAliases1 := SimulationCarAliasSlice(related)

	_, err = attachCarModelVariantSimulationCarAliases0(ctx, exec, len(related), simulationCarAliases1, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.SimulationCarAliases = append(carModelVariant0.R.SimulationCarAliases, simulationCarAliases1...)

	for _, rel := range related {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}

	return nil
}

func insertCarModelVariantTeams0(ctx context.Context, exec bob.Executor, teams1 []*TeamSetter, carModelVariant0 *CarModelVariant) (TeamSlice, error) {
	for i := range teams1 {
		teams1[i].CarModelVariantID = omitnull.From(carModelVariant0.ID)
	}

	ret, err := Teams.Insert(bob.ToMods(teams1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertCarModelVariantTeams0: %w", err)
	}

	return ret, nil
}

func attachCarModelVariantTeams0(ctx context.Context, exec bob.Executor, count int, teams1 TeamSlice, carModelVariant0 *CarModelVariant) (TeamSlice, error) {
	setter := &TeamSetter{
		CarModelVariantID: omitnull.From(carModelVariant0.ID),
	}

	err := teams1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachCarModelVariantTeams0: %w", err)
	}

	return teams1, nil
}

func (carModelVariant0 *CarModelVariant) InsertTeams(ctx context.Context, exec bob.Executor, related ...*TeamSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	teams1, err := insertCarModelVariantTeams0(ctx, exec, related, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.Teams = append(carModelVariant0.R.Teams, teams1...)

	for _, rel := range teams1 {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}
	return nil
}

func (carModelVariant0 *CarModelVariant) AttachTeams(ctx context.Context, exec bob.Executor, related ...*Team) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	teams1 := TeamSlice(related)

	_, err = attachCarModelVariantTeams0(ctx, exec, len(related), teams1, carModelVariant0)
	if err != nil {
		return err
	}

	carModelVariant0.R.Teams = append(carModelVariant0.R.Teams, teams1...)

	for _, rel := range related {
		rel.R.CarModelVariant = carModelVariant0
		rel.R.Loaded.CarModelVariant = true
	}

	return nil
}

type carModelVariantWhere[Q psql.Filterable] struct {
	ID         psql.WhereMod[Q, int32]
	CarModelID psql.WhereMod[Q, int32]
	Name       psql.WhereMod[Q, string]
	IsActive   psql.WhereMod[Q, bool]
	CreatedAt  psql.WhereMod[Q, time.Time]
	UpdatedAt  psql.WhereMod[Q, time.Time]
	CreatedBy  psql.WhereMod[Q, string]
	UpdatedBy  psql.WhereMod[Q, string]
}

func (carModelVariantWhere[Q]) AliasedAs(alias string) carModelVariantWhere[Q] {
	return buildCarModelVariantWhere[Q](buildCarModelVariantColumns(alias))
}

func buildCarModelVariantWhere[Q psql.Filterable](cols carModelVariantColumns) carModelVariantWhere[Q] {
	return carModelVariantWhere[Q]{
		ID:         psql.Where[Q, int32](cols.ID.Expression),
		CarModelID: psql.Where[Q, int32](cols.CarModelID.Expression),
		Name:       psql.Where[Q, string](cols.Name.Expression),
		IsActive:   psql.Where[Q, bool](cols.IsActive.Expression),
		CreatedAt:  psql.Where[Q, time.Time](cols.CreatedAt.Expression),
		UpdatedAt:  psql.Where[Q, time.Time](cols.UpdatedAt.Expression),
		CreatedBy:  psql.Where[Q, string](cols.CreatedBy.Expression),
		UpdatedBy:  psql.Where[Q, string](cols.UpdatedBy.Expression),
	}
}

func (o *CarModelVariant) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "CarClassesToCarModels":
		rels, ok := retrieved.(CarClassesToCarModelSlice)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.CarClassesToCarModels = rels
		o.R.Loaded.CarClassesToCarModels = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.CarModelVariant = o
				rel.R.Loaded.CarModelVariant = true
			}
		}
		return nil
	case "CarModel":
		rel, ok := retrieved.(*CarModel)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.CarModel = rel
		o.R.Loaded.CarModel = true

		if rel != nil {
			rel.R.CarModelVariants = CarModelVariantSlice{o}
		}
		return nil
	case "ResultEntries":
		rels, ok := retrieved.(ResultEntrySlice)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.ResultEntries = rels
		o.R.Loaded.ResultEntries = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.CarModelVariant = o
				rel.R.Loaded.CarModelVariant = true
			}
		}
		return nil
	case "SeasonCarModelVariants":
		rels, ok := retrieved.(SeasonCarModelVariantSlice)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.SeasonCarModelVariants = rels
		o.R.Loaded.SeasonCarModelVariants = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.CarModelVariant = o
				rel.R.Loaded.CarModelVariant = true
			}
		}
		return nil
	case "SeasonDrivers":
		rels, ok := retrieved.(SeasonDriverSlice)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.SeasonDrivers = rels
		o.R.Loaded.SeasonDrivers = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.CarModelVariant = o
				rel.R.Loaded.CarModelVariant = true
			}
		}
		return nil
	case "SimulationCarAliases":
		rels, ok := retrieved.(SimulationCarAliasSlice)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.SimulationCarAliases = rels
		o.R.Loaded.SimulationCarAliases = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.CarModelVariant = o
				rel.R.Loaded.CarModelVariant = true
			}
		}
		return nil
	case "Teams":
		rels, ok := retrieved.(TeamSlice)
		if !ok {
			return fmt.Errorf("carModelVariant cannot load %T as %q", retrieved, name)
		}

		o.R.Teams = rels
		o.R.Loaded.Teams = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.CarModelVariant = o
				rel.R.Loaded.CarModelVariant = true
			}
		}
		return nil
	default:
		return fmt.Errorf("carModelVariant has no relationship %q", name)
	}
}

type carModelVariantPreloader struct {
	CarModel func(...psql.PreloadOption) psql.Preloader
}

func buildCarModelVariantPreloader() carModelVariantPreloader {
	return carModelVariantPreloader{
		CarModel: func(opts ...psql.PreloadOption) psql.Preloader {
			return psql.Preload[*CarModel, CarModelSlice](psql.PreloadRel{
				Name: "CarModel",
				Sides: []psql.PreloadSide{
					{
						From:        CarModelVariants,
						To:          CarModels,
						FromColumns: []string{"car_model_id"},
						ToColumns:   []string{"id"},
					},
				},
			}, CarModels.Columns.Names(), opts...)
		},
	}
}

type carModelVariantThenLoader[Q orm.Loadable] struct {
	CarClassesToCarModels  func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	CarModel               func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	ResultEntries          func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SeasonCarModelVariants func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SeasonDrivers          func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SimulationCarAliases   func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Teams                  func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildCarModelVariantThenLoader[Q orm.Loadable]() carModelVariantThenLoader[Q] {
	type CarClassesToCarModelsLoadInterface interface {
		LoadCarClassesToCarModels(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type CarModelLoadInterface interface {
		LoadCarModel(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type ResultEntriesLoadInterface interface {
		LoadResultEntries(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SeasonCarModelVariantsLoadInterface interface {
		LoadSeasonCarModelVariants(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SeasonDriversLoadInterface interface {
		LoadSeasonDrivers(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SimulationCarAliasesLoadInterface interface {
		LoadSimulationCarAliases(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type TeamsLoadInterface interface {
		LoadTeams(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return carModelVariantThenLoader[Q]{
		CarClassesToCarModels: thenLoadBuilder[Q](
			"CarClassesToCarModels",
			func(ctx context.Context, exec bob.Executor, retrieved CarClassesToCarModelsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCarClassesToCarModels(ctx, exec, mods...)
			},
		),
		CarModel: thenLoadBuilder[Q](
			"CarModel",
			func(ctx context.Context, exec bob.Executor, retrieved CarModelLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCarModel(ctx, exec, mods...)
			},
		),
		ResultEntries: thenLoadBuilder[Q](
			"ResultEntries",
			func(ctx context.Context, exec bob.Executor, retrieved ResultEntriesLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadResultEntries(ctx, exec, mods...)
			},
		),
		SeasonCarModelVariants: thenLoadBuilder[Q](
			"SeasonCarModelVariants",
			func(ctx context.Context, exec bob.Executor, retrieved SeasonCarModelVariantsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadSeasonCarModelVariants(ctx, exec, mods...)
			},
		),
		SeasonDrivers: thenLoadBuilder[Q](
			"SeasonDrivers",
			func(ctx context.Context, exec bob.Executor, retrieved SeasonDriversLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadSeasonDrivers(ctx, exec, mods...)
			},
		),
		SimulationCarAliases: thenLoadBuilder[Q](
			"SimulationCarAliases",
			func(ctx context.Context, exec bob.Executor, retrieved SimulationCarAliasesLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadSimulationCarAliases(ctx, exec, mods...)
			},
		),
		Teams: thenLoadBuilder[Q](
			"Teams",
			func(ctx context.Context, exec bob.Executor, retrieved TeamsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadTeams(ctx, exec, mods...)
			},
		),
	}
}

// LoadCarClassesToCarModels loads the carModelVariant's CarClassesToCarModels into the .R struct
func (o *CarModelVariant) LoadCarClassesToCarModels(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.CarClassesToCarModels = nil
	o.R.Loaded.CarClassesToCarModels = false

	related, err := o.CarClassesToCarModels(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.CarModelVariant = o
		rel.R.Loaded.CarModelVariant = true
	}

	o.R.CarClassesToCarModels = related
	o.R.Loaded.CarClassesToCarModels = true
	return nil
}

// LoadCarClassesToCarModels loads the carModelVariant's CarClassesToCarModels into the .R struct
func (os CarModelVariantSlice) LoadCarClassesToCarModels(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	carClassesToCarModels, err := os.CarClassesToCarModels(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.CarClassesToCarModels = nil
		o.R.Loaded.CarClassesToCarModels = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.ID] = append(carModelVariantByKey[o.ID], o)
	}

	for _, rel := range carClassesToCarModels {

		if !rel.CarModelVariantID.IsValue() {
			continue
		}

		owners, ok := carModelVariantByKey[rel.CarModelVariantID.MustGet()]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.CarModelVariant = o
			rel.R.Loaded.CarModelVariant = true

			o.R.CarClassesToCarModels = append(o.R.CarClassesToCarModels, rel)

		}
	}

	return nil
}

// LoadCarModel loads the carModelVariant's CarModel into the .R struct
func (o *CarModelVariant) LoadCarModel(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.CarModel = nil
	o.R.Loaded.CarModel = false

	related, err := o.CarModel(mods...).One(ctx, exec)
	if err != nil {
		return err
	}

	related.R.CarModelVariants = CarModelVariantSlice{o}

	o.R.CarModel = related
	o.R.Loaded.CarModel = true
	return nil
}

// LoadCarModel loads the carModelVariant's CarModel into the .R struct
func (os CarModelVariantSlice) LoadCarModel(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	carModels, err := os.CarModel(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.CarModel = nil
		o.R.Loaded.CarModel = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.CarModelID] = append(carModelVariantByKey[o.CarModelID], o)
	}

	for _, rel := range carModels {

		owners, ok := carModelVariantByKey[rel.ID]
		if !ok {
			continue
		}

		for _, o := range owners {

			// to-one: keep only the first matching child (matches the previous break)
			if o.R.CarModel != nil {
				continue
			}

			rel.R.CarModelVariants = append(rel.R.CarModelVariants, o)

			o.R.CarModel = rel

		}
	}

	return nil
}

// LoadResultEntries loads the carModelVariant's ResultEntries into the .R struct
func (o *CarModelVariant) LoadResultEntries(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.ResultEntries = nil
	o.R.Loaded.ResultEntries = false

	related, err := o.ResultEntries(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.CarModelVariant = o
		rel.R.Loaded.CarModelVariant = true
	}

	o.R.ResultEntries = related
	o.R.Loaded.ResultEntries = true
	return nil
}

// LoadResultEntries loads the carModelVariant's ResultEntries into the .R struct
func (os CarModelVariantSlice) LoadResultEntries(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	resultEntries, err := os.ResultEntries(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.ResultEntries = nil
		o.R.Loaded.ResultEntries = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.ID] = append(carModelVariantByKey[o.ID], o)
	}

	for _, rel := range resultEntries {

		if !rel.CarModelVariantID.IsValue() {
			continue
		}

		owners, ok := carModelVariantByKey[rel.CarModelVariantID.MustGet()]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.CarModelVariant = o
			rel.R.Loaded.CarModelVariant = true

			o.R.ResultEntries = append(o.R.ResultEntries, rel)

		}
	}

	return nil
}

// LoadSeasonCarModelVariants loads the carModelVariant's SeasonCarModelVariants into the .R struct
func (o *CarModelVariant) LoadSeasonCarModelVariants(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.SeasonCarModelVariants = nil
	o.R.Loaded.SeasonCarModelVariants = false

	related, err := o.SeasonCarModelVariants(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.CarModelVariant = o
		rel.R.Loaded.CarModelVariant = true
	}

	o.R.SeasonCarModelVariants = related
	o.R.Loaded.SeasonCarModelVariants = true
	return nil
}

// LoadSeasonCarModelVariants loads the carModelVariant's SeasonCarModelVariants into the .R struct
func (os CarModelVariantSlice) LoadSeasonCarModelVariants(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	seasonCarModelVariants, err := os.SeasonCarModelVariants(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.SeasonCarModelVariants = nil
		o.R.Loaded.SeasonCarModelVariants = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.ID] = append(carModelVariantByKey[o.ID], o)
	}

	for _, rel := range seasonCarModelVariants {

		owners, ok := carModelVariantByKey[rel.CarModelVariantID]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.CarModelVariant = o
			rel.R.Loaded.CarModelVariant = true

			o.R.SeasonCarModelVariants = append(o.R.SeasonCarModelVariants, rel)

		}
	}

	return nil
}

// LoadSeasonDrivers loads the carModelVariant's SeasonDrivers into the .R struct
func (o *CarModelVariant) LoadSeasonDrivers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.SeasonDrivers = nil
	o.R.Loaded.SeasonDrivers = false

	related, err := o.SeasonDrivers(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.CarModelVariant = o
		rel.R.Loaded.CarModelVariant = true
	}

	o.R.SeasonDrivers = related
	o.R.Loaded.SeasonDrivers = true
	return nil
}

// LoadSeasonDrivers loads the carModelVariant's SeasonDrivers into the .R struct
func (os CarModelVariantSlice) LoadSeasonDrivers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	seasonDrivers, err := os.SeasonDrivers(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.SeasonDrivers = nil
		o.R.Loaded.SeasonDrivers = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.ID] = append(carModelVariantByKey[o.ID], o)
	}

	for _, rel := range seasonDrivers {

		owners, ok := carModelVariantByKey[rel.CarModelVariantID]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.CarModelVariant = o
			rel.R.Loaded.CarModelVariant = true

			o.R.SeasonDrivers = append(o.R.SeasonDrivers, rel)

		}
	}

	return nil
}

// LoadSimulationCarAliases loads the carModelVariant's SimulationCarAliases into the .R struct
func (o *CarModelVariant) LoadSimulationCarAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.SimulationCarAliases = nil
	o.R.Loaded.SimulationCarAliases = false

	related, err := o.SimulationCarAliases(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.CarModelVariant = o
		rel.R.Loaded.CarModelVariant = true
	}

	o.R.SimulationCarAliases = related
	o.R.Loaded.SimulationCarAliases = true
	return nil
}

// LoadSimulationCarAliases loads the carModelVariant's SimulationCarAliases into the .R struct
func (os CarModelVariantSlice) LoadSimulationCarAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	simulationCarAliases, err := os.SimulationCarAliases(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.SimulationCarAliases = nil
		o.R.Loaded.SimulationCarAliases = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.ID] = append(carModelVariantByKey[o.ID], o)
	}

	for _, rel := range simulationCarAliases {

		owners, ok := carModelVariantByKey[rel.CarModelVariantID]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.CarModelVariant = o
			rel.R.Loaded.CarModelVariant = true

			o.R.SimulationCarAliases = append(o.R.SimulationCarAliases, rel)

		}
	}

	return nil
}

// LoadTeams loads the carModelVariant's Teams into the .R struct
func (o *CarModelVariant) LoadTeams(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Teams = nil
	o.R.Loaded.Teams = false

	related, err := o.Teams(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.CarModelVariant = o
		rel.R.Loaded.CarModelVariant = true
	}

	o.R.Teams = related
	o.R.Loaded.Teams = true
	return nil
}

// LoadTeams loads the carModelVariant's Teams into the .R struct
func (os CarModelVariantSlice) LoadTeams(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	teams, err := os.Teams(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.Teams = nil
		o.R.Loaded.Teams = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	carModelVariantByKey := make(map[int32][]*CarModelVariant, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		carModelVariantByKey[o.ID] = append(carModelVariantByKey[o.ID], o)
	}

	for _, rel := range teams {

		if !rel.CarModelVariantID.IsValue() {
			continue
		}

		owners, ok := carModelVariantByKey[rel.CarModelVariantID.MustGet()]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.CarModelVariant = o
			rel.R.Loaded.CarModelVariant = true

			o.R.Teams = append(o.R.Teams, rel)

		}
	}

	return nil
}

// carModelVariantC is where relationship counts are stored.
type carModelVariantC struct {
	CarClassesToCarModels  *int64
	ResultEntries          *int64
	SeasonCarModelVariants *int64
	SeasonDrivers          *int64
	SimulationCarAliases   *int64
	Teams                  *int64
}

// PreloadCount sets a count in the C struct by name
func (o *CarModelVariant) PreloadCount(name string, count int64) error {
	if o == nil {
		return nil
	}

	switch name {
	case "CarClassesToCarModels":
		o.C.CarClassesToCarModels = &count
	case "ResultEntries":
		o.C.ResultEntries = &count
	case "SeasonCarModelVariants":
		o.C.SeasonCarModelVariants = &count
	case "SeasonDrivers":
		o.C.SeasonDrivers = &count
	case "SimulationCarAliases":
		o.C.SimulationCarAliases = &count
	case "Teams":
		o.C.Teams = &count
	}
	return nil
}

type carModelVariantCountPreloader struct {
	CarClassesToCarModels  func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	ResultEntries          func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	SeasonCarModelVariants func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	SeasonDrivers          func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	SimulationCarAliases   func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	Teams                  func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
}

func buildCarModelVariantCountPreloader() carModelVariantCountPreloader {
	return carModelVariantCountPreloader{
		CarClassesToCarModels: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*CarModelVariant]("CarClassesToCarModels", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = CarModelVariants.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(CarClassesToCarModels.NameAsExpr()),
					sm.Where(psql.Quote(CarClassesToCarModels.Alias(), "car_model_variant_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		ResultEntries: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*CarModelVariant]("ResultEntries", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = CarModelVariants.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(ResultEntries.NameAsExpr()),
					sm.Where(psql.Quote(ResultEntries.Alias(), "car_model_variant_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		SeasonCarModelVariants: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*CarModelVariant]("SeasonCarModelVariants", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = CarModelVariants.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(SeasonCarModelVariants.NameAsExpr()),
					sm.Where(psql.Quote(SeasonCarModelVariants.Alias(), "car_model_variant_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		SeasonDrivers: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*CarModelVariant]("SeasonDrivers", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = CarModelVariants.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(SeasonDrivers.NameAsExpr()),
					sm.Where(psql.Quote(SeasonDrivers.Alias(), "car_model_variant_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		SimulationCarAliases: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*CarModelVariant]("SimulationCarAliases", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = CarModelVariants.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(SimulationCarAliases.NameAsExpr()),
					sm.Where(psql.Quote(SimulationCarAliases.Alias(), "car_model_variant_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		Teams: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*CarModelVariant]("Teams", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = CarModelVariants.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(Teams.NameAsExpr()),
					sm.Where(psql.Quote(Teams.Alias(), "car_model_variant_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
	}
}

type carModelVariantCountThenLoader[Q orm.Loadable] struct {
	CarClassesToCarModels  func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	ResultEntries          func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SeasonCarModelVariants func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SeasonDrivers          func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	SimulationCarAliases   func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Teams                  func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildCarModelVariantCountThenLoader[Q orm.Loadable]() carModelVariantCountThenLoader[Q] {
	type CarClassesToCarModelsCountInterface interface {
		LoadCountCarClassesToCarModels(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type ResultEntriesCountInterface interface {
		LoadCountResultEntries(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SeasonCarModelVariantsCountInterface interface {
		LoadCountSeasonCarModelVariants(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SeasonDriversCountInterface interface {
		LoadCountSeasonDrivers(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type SimulationCarAliasesCountInterface interface {
		LoadCountSimulationCarAliases(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type TeamsCountInterface interface {
		LoadCountTeams(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return carModelVariantCountThenLoader[Q]{
		CarClassesToCarModels: countThenLoadBuilder[Q](
			"CarClassesToCarModels",
			func(ctx context.Context, exec bob.Executor, retrieved CarClassesToCarModelsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountCarClassesToCarModels(ctx, exec, mods...)
			},
		),
		ResultEntries: countThenLoadBuilder[Q](
			"ResultEntries",
			func(ctx context.Context, exec bob.Executor, retrieved ResultEntriesCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountResultEntries(ctx, exec, mods...)
			},
		),
		SeasonCarModelVariants: countThenLoadBuilder[Q](
			"SeasonCarModelVariants",
			func(ctx context.Context, exec bob.Executor, retrieved SeasonCarModelVariantsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountSeasonCarModelVariants(ctx, exec, mods...)
			},
		),
		SeasonDrivers: countThenLoadBuilder[Q](
			"SeasonDrivers",
			func(ctx context.Context, exec bob.Executor, retrieved SeasonDriversCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountSeasonDrivers(ctx, exec, mods...)
			},
		),
		SimulationCarAliases: countThenLoadBuilder[Q](
			"SimulationCarAliases",
			func(ctx context.Context, exec bob.Executor, retrieved SimulationCarAliasesCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountSimulationCarAliases(ctx, exec, mods...)
			},
		),
		Teams: countThenLoadBuilder[Q](
			"Teams",
			func(ctx context.Context, exec bob.Executor, retrieved TeamsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountTeams(ctx, exec, mods...)
			},
		),
	}
}

// LoadCountCarClassesToCarModels loads the count of CarClassesToCarModels into the C struct
func (o *CarModelVariant) LoadCountCarClassesToCarModels(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.CarClassesToCarModels(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.CarClassesToCarModels = &count
	return nil
}

// LoadCountCarClassesToCarModels loads the count of CarClassesToCarModels for a slice in a single batch query
func (os CarModelVariantSlice) LoadCountCarClassesToCarModels(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			CarClassesToCarModels.Columns.CarModelVariantID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(CarClassesToCarModels.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(CarClassesToCarModels.Columns.CarModelVariantID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(CarClassesToCarModels.Columns.CarModelVariantID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.CarClassesToCarModels = &count
	}

	return nil
}

// LoadCountResultEntries loads the count of ResultEntries into the C struct
func (o *CarModelVariant) LoadCountResultEntries(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.ResultEntries(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.ResultEntries = &count
	return nil
}

// LoadCountResultEntries loads the count of ResultEntries for a slice in a single batch query
func (os CarModelVariantSlice) LoadCountResultEntries(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			ResultEntries.Columns.CarModelVariantID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(ResultEntries.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(ResultEntries.Columns.CarModelVariantID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(ResultEntries.Columns.CarModelVariantID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.ResultEntries = &count
	}

	return nil
}

// LoadCountSeasonCarModelVariants loads the count of SeasonCarModelVariants into the C struct
func (o *CarModelVariant) LoadCountSeasonCarModelVariants(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.SeasonCarModelVariants(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.SeasonCarModelVariants = &count
	return nil
}

// LoadCountSeasonCarModelVariants loads the count of SeasonCarModelVariants for a slice in a single batch query
func (os CarModelVariantSlice) LoadCountSeasonCarModelVariants(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			SeasonCarModelVariants.Columns.CarModelVariantID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(SeasonCarModelVariants.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(SeasonCarModelVariants.Columns.CarModelVariantID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(SeasonCarModelVariants.Columns.CarModelVariantID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.SeasonCarModelVariants = &count
	}

	return nil
}

// LoadCountSeasonDrivers loads the count of SeasonDrivers into the C struct
func (o *CarModelVariant) LoadCountSeasonDrivers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.SeasonDrivers(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.SeasonDrivers = &count
	return nil
}

// LoadCountSeasonDrivers loads the count of SeasonDrivers for a slice in a single batch query
func (os CarModelVariantSlice) LoadCountSeasonDrivers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			SeasonDrivers.Columns.CarModelVariantID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(SeasonDrivers.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(SeasonDrivers.Columns.CarModelVariantID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(SeasonDrivers.Columns.CarModelVariantID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.SeasonDrivers = &count
	}

	return nil
}

// LoadCountSimulationCarAliases loads the count of SimulationCarAliases into the C struct
func (o *CarModelVariant) LoadCountSimulationCarAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.SimulationCarAliases(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.SimulationCarAliases = &count
	return nil
}

// LoadCountSimulationCarAliases loads the count of SimulationCarAliases for a slice in a single batch query
func (os CarModelVariantSlice) LoadCountSimulationCarAliases(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			SimulationCarAliases.Columns.CarModelVariantID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(SimulationCarAliases.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(SimulationCarAliases.Columns.CarModelVariantID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(SimulationCarAliases.Columns.CarModelVariantID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.SimulationCarAliases = &count
	}

	return nil
}

// LoadCountTeams loads the count of Teams into the C struct
func (o *CarModelVariant) LoadCountTeams(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.Teams(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.Teams = &count
	return nil
}

// LoadCountTeams loads the count of Teams for a slice in a single batch query
func (os CarModelVariantSlice) LoadCountTeams(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			Teams.Columns.CarModelVariantID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(Teams.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(Teams.Columns.CarModelVariantID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(Teams.Columns.CarModelVariantID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.Teams = &count
	}

	return nil
}

type carModelVariantJoins[Q dialect.Joinable] struct {
	typ                    string
	CarClassesToCarModels  modAs[Q, carClassesToCarModelColumns]
	CarModel               modAs[Q, carModelColumns]
	ResultEntries          modAs[Q, resultEntryColumns]
	SeasonCarModelVariants modAs[Q, seasonCarModelVariantColumns]
	SeasonDrivers          modAs[Q, seasonDriverColumns]
	SimulationCarAliases   modAs[Q, simulationCarAliasColumns]
	Teams                  modAs[Q, teamColumns]
}

func (j carModelVariantJoins[Q]) aliasedAs(alias string) carModelVariantJoins[Q] {
	return buildCarModelVariantJoins[Q](buildCarModelVariantColumns(alias), j.typ)
}

func buildCarModelVariantJoins[Q dialect.Joinable](cols carModelVariantColumns, typ string) carModelVariantJoins[Q] {
	return carModelVariantJoins[Q]{
		typ: typ,
		CarClassesToCarModels: modAs[Q, carClassesToCarModelColumns]{
			c: CarClassesToCarModels.Columns,
			f: func(to carClassesToCarModelColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, CarClassesToCarModels.NameExpr().As(to.Alias())).On(
						to.CarModelVariantID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		CarModel: modAs[Q, carModelColumns]{
			c: CarModels.Columns,
			f: func(to carModelColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, CarModels.NameExpr().As(to.Alias())).On(
						to.ID.EQ(cols.CarModelID),
					))
				}

				return mods
			},
		},
		ResultEntries: modAs[Q, resultEntryColumns]{
			c: ResultEntries.Columns,
			f: func(to resultEntryColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, ResultEntries.NameExpr().As(to.Alias())).On(
						to.CarModelVariantID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		SeasonCarModelVariants: modAs[Q, seasonCarModelVariantColumns]{
			c: SeasonCarModelVariants.Columns,
			f: func(to seasonCarModelVariantColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, SeasonCarModelVariants.NameExpr().As(to.Alias())).On(
						to.CarModelVariantID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		SeasonDrivers: modAs[Q, seasonDriverColumns]{
			c: SeasonDrivers.Columns,
			f: func(to seasonDriverColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, SeasonDrivers.NameExpr().As(to.Alias())).On(
						to.CarModelVariantID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		SimulationCarAliases: modAs[Q, simulationCarAliasColumns]{
			c: SimulationCarAliases.Columns,
			f: func(to simulationCarAliasColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, SimulationCarAliases.NameExpr().As(to.Alias())).On(
						to.CarModelVariantID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		Teams: modAs[Q, teamColumns]{
			c: Teams.Columns,
			f: func(to teamColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Teams.NameExpr().As(to.Alias())).On(
						to.CarModelVariantID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
	}
}
